# 项目交接 · 正式课教学监控系统

> **给谁看**：新开的 AI 会话 / 接手者。读完本文件 + `AGENTS.md` + `待办清单.md` 即可无缝接手。
> **本文件状态**：快照式交接（点对点），接手事项办完可删，或持续更新。
> **项目定位**：顷悟教学监控系统（正式课主线），Python 单服务，三端（学生/教师/大屏）。
> **新会话入口**：对 AI 说 **"做正式课"**，它按 `AGENTS.md` 启动协议自动读本文件。

---

## 本机位置速查

- 代码目录：`ar-class-monitor-demo\`（根：`C:\Users\Administrator\Documents\Default Project\ar-class-monitor-demo`）
- 运行方式：`python server.py 8099`（本机）
- 页面：学生 `/student/`、教师 `/monitor/`、大屏 `/bigscreen/`，各课 `/warmup/ /game/ /tool/ /agent-team/`
- 架构详见 `AGENTS.md` 三章（三端+服务、数据链路、课堂场次与签到）

---

## 〇、最近交接状态

**当前状态**：近期未大动，以 `AGENTS.md` + `待办清单.md` 为准。

- 三端已按**三项目**改造：学生/教师/大屏 tab = 项目一(接金币游戏, course 1) / 项目二(心情电量, course 2) / 项目三(内心戏, course 4)。
- 预备课（教"把话说清楚"）已停用为独立 tab，`warmup/` 代码与数据保留，内容作为正式课开场集中讲。
- 数据链路：顷悟聊天面板 → `qingwu-dialog-bridge.py` 转发；作品内对话 → `lib/qw-chat-fwd.js`；操作 → `track()`。统一事件流 → `lib/analyze.js` 穿透判定。
- 学习水位：`water-analyzer.py` 常驻（规则 + DeepSeek AI 深度解析，失败回退规则）→ `agent-live/water.json`。

---

## 一、一句话现状

**顷悟教学监控系统 = 正式课主线**。课堂上学生用顷悟平台做 AI 应用，教师在后台实时看理解度，大屏匿名投影。**主战场，重点维护。**

---

## 二、怎么跑

```
cd ar-class-monitor-demo
python server.py 8099        （或 powershell -ExecutionPolicy Bypass -File start_server.ps1 8099）
```

页面：学生端 `/student/`，教师端 `/monitor/`，大屏 `/bigscreen/`，各课 `/warmup/ /game/ /tool/ /agent-team/`。

---

## 三、⚠️ 未提交的改动

见 `待办清单.md` 与 git status。近期以文档为主（AGENTS.md 新增省 token 纪律等），代码未大动。

---

## 四、关键文件速查

| 文件 | 作用 |
|---|---|
| `server.py` | 静态文件 + 数据服务（/api/collect、/api/events、/api/class、场次/签到） |
| `lib/analyze.js` | 穿透判定统一入口（gridFor/detectGap/paraphrase） |
| `lib/track.js` | 埋点模块（作品内一行接入） |
| `lib/qw-chat-fwd.js` | 作品内对话转发（包装 ai.chat） |
| `qingwu-dialog-bridge.py` | 顷悟聊天面板对话转发桥 |
| `water-analyzer.py` | 学习水位分析服务（规则+DeepSeek，失败回退） |
| `agent-live/events.jsonl` | 统一事件流 |
| `正式课文档/` | 课程方案/教案/知识点分类等（详见 `文档索引.md`） |

---

## 五、接手第一步建议

1. 先读 `AGENTS.md`（目录/架构/铁律）→ `待办清单.md`（全部待办）→ 本文件
2. 对照 `正式课文档/课程方案.md` 确认三项目现状
3. 问张老师下一步做什么
