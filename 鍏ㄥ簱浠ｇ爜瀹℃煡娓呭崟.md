# 全库代码审查 · 完整问题清单

> 审查时间：2026-08-04
> 审查范围：src/ 全部 58 个文件（48 .ts + 10 .tsx）+ prisma/schema + next.config
> 审查方式：4 路并行深度精读 + 核心文件交叉验证，逐行核查
> 本清单已去重，按严重性分级。共发现 **62 个问题**：P0 × 16，P1 × 25，P2 × 21

---

## 一、P0 严重问题（影响功能正确性 / 数据一致性 / 会直接卡死，必须修）

### P0-1 数据库连接池被设成 1（元凶，"慢得惊人"的根因）
- **位置**：`.env` 的 `DATABASE_URL`，`connection_limit=1&pool_timeout=10`
- **现象**：dev.log 大量请求正好 ~10050ms 返回 404/500；`join` 50 秒、创建课堂 36 秒、control 20-43 秒
- **根因**：同一时间只允许 1 个 DB 查询，并发请求全部排队 10 秒超时
- **建议**：`connection_limit=1` → `10`

### P0-2 resetClassroom / endClassroom 多步删除无事务
- **位置**：`classroom.ts:109-140`（reset）、`classroom.ts:146-172`（end）
- **问题**：删除 moduleProgress→a0Screening→consentRecord→participant→classInvitation 串行执行无 `prisma.$transaction`，中途失败留下半删数据
- **建议**：用 `$transaction` 包裹

### P0-3 joinClassroom 邀请码 check-then-use 竞态
- **位置**：`classroom.ts:196-269`
- **问题**：先查 `invitation.used`（214行）再后续 `update used:true`（238行），中间无锁，并发两个学生用同一邀请码会都通过检查、都被标记使用
- **建议**：事务包裹，或用条件更新 `updateMany({ where: { id, used: false }, data: { used: true }})` 检查 count

### P0-4 getSessionState 缓存写操作后不失效（数据过期）
- **位置**：`classroom.ts:530-565`（缓存）、写操作 `submitModule`/`submitScreening`/`submitScreeningFollowup`/`upsertProgress` 等均未调 `invalidateSessionCache`
- **现状**：只有 `control/route.ts` 调了 `invalidateSessionCache`；学生提交后教师端 30 秒内看不到新进度
- **建议**：所有状态变更函数末尾（emitProgress 之前/之后）调 `invalidateSessionCache(sessionId)`

### P0-5 学生端 visibilitychange 新建 EventSource 无回调（SSE 事件全丢）
- **位置**：`student/page.tsx:186-190`
- **问题**：页面可见性变化时 `new EventSource(...)` 但没设 `onmessage`/`onerror`，是无效连接，后续 SSE 事件全丢且不会重连
- **建议**：复用 `connect()` 函数

### P0-6 教师端 loadState 失败后 checking 永远 true（页面卡在"正在检查"）
- **位置**：`teacher/page.tsx:108-135` + `:275-294`
- **问题**：`saved` 分支调 `loadState(saved)`，失败走 catch 会 `setSessionId('')` 但没 `setChecking(false)`，只有 `!saved` 分支的 finally 才设；导致页面永远转圈
- **建议**：catch 分支补 `setChecking(false)`

### P0-7 大屏 load 函数无 try/catch（网络异常静默白屏）
- **位置**：`screen/page.tsx:79`（初始校验）、`:123`（轮询）、`:138`（load 函数体）
- **问题**：`await (await fetch(...)).json()` 无错误处理，fetch 失败/JSON 解析失败时整个 async IIFE 中断，后续 load 和 SSE 都不执行
- **建议**：包裹 try/catch，失败显示错误状态

### P0-8 所有 fetch 调用几乎都不检查 res.ok（全库系统性问题）
- **位置**：`TeacherFinale.tsx:33,70`、`StudentFinale.tsx:100,142,172`、`ScreenFinale.tsx:22`、`L2StudentFlow.tsx:89,150`、`screen/page.tsx:328,553` 等十余处
- **问题**：服务端返回 4xx/5xx 时直接 `res.json()` 会抛异常或得到错误结构，被 catch 静默吞掉，用户完全无感知
- **建议**：统一加 `if (!res.ok) throw` 或检查后处理

### P0-9 StudentFinale timerRef 组件卸载时未清理（内存泄漏 + setState on unmounted）
- **位置**：`StudentFinale.tsx:62,157-165`
- **问题**：step 揭示定时器只在 openVisit 或 i>=st.length 时清理，组件卸载时若仍在运行会泄漏
- **建议**：useEffect cleanup 里 `clearInterval(timerRef.current)`

### P0-10 qrcode / network / settings/llm 接口无 try-catch
- **位置**：`classroom/[id]/qrcode/route.ts:8-44`、`network/route.ts:4-15`、`settings/llm/route.ts:4-12`
- **问题**：`QRCode.toDataURL`、`os.networkInterfaces()`、`fs.readFileSync` 抛异常时未捕获 500
- **建议**：加 try-catch

### P0-11 错误响应结构不统一
- **位置**：`classroom/latest/route.ts:13`（`{error: String(err)}`）、`settings/llm/route.ts:44`（`{error:'保存失败', message}`）
- **问题**：与其他接口统一的 `{error:{code,message}}` 不一致，前端解析逻辑会错乱
- **建议**：统一为 `{error:{code,message}}`

### P0-12 wechat getWechatUser 无超时 + secret 拼在 URL
- **位置**：`wechat.ts:37-58`、`wechat/oauth/callback/route.ts:17`
- **问题**：两个 `fetch` 无超时，微信 API 慢时请求永久挂起；`secret` 拼在 query string 会出现在服务器日志
- **建议**：加 `AbortController`；改用 POST body 传 secret

### P0-13 control/join/resume/finale 错误码一刀切 400
- **位置**：`classroom/[id]/control/route.ts:44`、`student/join/route.ts:37`、`student/resume/route.ts:12`、`finale/state/route.ts:66`
- **问题**：DB 故障等未知异常也返 400，前端无法区分业务错误和服务器故障
- **建议**：已知业务错误（SESSION_NOT_FOUND 等）4xx，未知异常 500

### P0-14 ensureTemplate 每次写库（高频热点）
- **位置**：`courseConfig.ts:696-717`
- **问题**：被 classroom.ts 几乎所有操作调用，每次都 `findFirst+create/update`，无内存缓存，是 DB 热点
- **建议**：模块级缓存 template id，仅首次读 DB

### P0-15 schema 三张表缺外键关系（无法级联清理）
- **位置**：`schema.prisma:148-186`
- **问题**：`AgentCompany`、`FinaleState`、`CompanyFeedback` 的 `sessionId` 都无 `@relation` 到 ClassSession，删除课堂时变孤儿记录
- **建议**：补关系定义 + `onDelete: Cascade`

### P0-16 StudentFinale 各处 fetch 未检查 res.ok（重申，影响发布/反馈）
- **位置**：`StudentFinale.tsx:100`（publish）、`:142`（runVisit）、`:172`（submitFeedback）
- **问题**：学生发布公司/运行/反馈失败时完全无提示
- **建议**：检查 res.ok，失败给 toast

---

## 二、P1 重要问题（影响健壮性 / 体验 / 并发安全，应修）

### 性能 / 并发
- **P1-1** finale 轮询过频：`TeacherFinale.tsx:43` 每 2.5s、`ScreenFinale.tsx:33` 每 3s、`StudentFinale.tsx:74` 每 3s → 改 10s（SSE 已推送）
- **P1-2** `student/[anonymousId]/route.ts:9-25` 6-7 次串行 DB 查询，且 `getStudentAiStyle` 重复查 moduleProgress → Promise.all 并行化
- **P1-3** `analytics/route.ts`、`screening/analytics`、`screening/sales-brief` 三个分析接口无缓存 → 加 30s 内存缓存
- **P1-4** `finale.ts:71-112` runCompanyChain 串行调多个 LLM，每个默认 60s 超时，4 个 Agent 最差 240s → 降超时或流式
- **P1-5** 教师端 SSE `progress:update` 每次触发 `fetchInvitations`（`teacher/page.tsx:159`），高频事件产生请求风暴 → debounce
- **P1-6** 大屏 SSE `analytics:update` 触发 `fetchAnalytics` + `fetchScreening` 并发无去重（`screen/page.tsx:108`）→ debounce
- **P1-7** `finale/company/route.ts:68-72` 用 `await import('@/lib/db')` 动态导入 → 改静态 import
- **P1-8** `ids.ts:22-29` generateAnonymousId 用 count+1 序号，高并发可能重试/死循环 → 原子生成

### 错误处理 / 健壮性
- **P1-9** `classroom.ts:16,530` getSessionState 缓存是内存级 Map，`realtime.ts` 事件总线也是内存级 → 多实例部署不跨实例（至少加启动告警）
- **P1-10** 三端 SSE 断连后固定 3s 重连无指数退避：`student:185`、`teacher:185`、`screen:115` → 3s/6s/12s/最大 60s
- **P1-11** `screen/page.tsx:553` A03Screen fetch baseline 失败静默 `.catch(()=>{})`，界面一直"加载中"无提示
- **P1-12** `L2StudentFlow.tsx:107` save 失败完全静默，乐观更新不回滚 → 至少 toast
- **P1-13** 多处 `await req.json()` 无 `.catch()` 兜底：`l2/process:17,48`、`l2/check:9`、`l2/run:14`、`screening/followup:6`、`module/submit:6`、`l2/process GET` 整个无 try-catch
- **P1-14** `serverEnv.ts:50-58` API Key 明文写 `.env.local` 且无文件权限；`:61-98` 写入无并发控制可能损坏文件
- **P1-15** `teacher/page.tsx:285` loadState catch 递归调用自身，无深度限制
- **P1-16** `classroom.ts:593` openFinaleRound 先读再 update，TOCTOU 竞态 → 用 `round: { increment: 1 }` 原子更新
- **P1-17** `classroom.ts:22` audit 失败完全静默 `.catch(()=>{})`，审计日志丢失无感知 → 至少 console.error
- **P1-18** 多处 localStorage 访问无 try/catch（隐私模式会抛错）：`student/page.tsx:99,290`、`teacher/page.tsx:201,260`
- **P1-19** `wechat/oauth/callback/route.ts:22` catch 重定向 URL 未 encodeURIComponent（与 :18 不一致）
- **P1-20** `events/[sessionId]/route.ts:12-43` SSE start 回调无 try-catch 包裹 subscribe
- **P1-21** `StudentFinale.tsx:130` runVisit 防重入用闭包 running state（可能旧值）→ 用 useRef
- **P1-22** `StudentFinale.tsx:127` openVisit 切公司只 clearInterval 未 setRunning(false)
- **P1-23** `screen/page.tsx:137`、`student/page.tsx:301` load/refreshCurrent 无竞态保护（AbortController/序号）
- **P1-24** `llm.ts:33-34` AbortController 超时后未区分 AbortError
- **P1-25** `ScreenFinale.tsx:42` finale:working 事件每次新建 doneSteps:[] 覆盖已完成步骤

### 逻辑
- **P1-26** `classroom.ts:82` advanceClassroom 已是最后模块时 `next` 回退为 currentModuleId（原地不动）但仍发 `module:advanced` 事件，前端误判 → 检测到末尾抛错或返不同事件

---

## 三、P2 次要问题（优化 / 规范 / 一致性，可延后）

### 安全 / 校验
- **P2-1** 全库无身份校验：`createClassroom` teacherId 默认 'teacher-default'、`studentAnalyst.ts` 暴露学生隐私数据无门槛、`getCompany` 不校验 company 归属 session、`submitFinaleFeedback` 不校验 visitor 是否真实参与者/是否重复提交
- **P2-2** `classroom.ts:304` resumeSession 未检查 session 是否已 closed，已关闭课堂学生仍可 resume 并标记 connected:true
- **P2-3** `classroom.ts:349` submitModule 只检查 moduleLocked，未检查 session.status===active，已关闭课堂仍可提交
- **P2-4** `classroom.ts:62/94` startClassroom/jumpClassroom 未校验 session 当前状态
- **P2-5** `classroom.ts:271-274` makeInvitationCode 用 Math.random()（非加密安全），与 ids.ts 的 crypto 实现不一致 → 统一用 ids.ts
- **P2-6** `screening.ts:259` aiLabel 直接 `as AiLabel` 强转，非法值会导致 labels 计算错乱 → 先校验

### 性能 / 查询优化
- **P2-7** `getProgressSummary` 仍重复查 session+template（getSessionState 已查过）→ 上层传入复用
- **P2-8** `analytics.ts:202` include participant 拉全行只用 anonymousId → select 精简
- **P2-9** `analytics.ts:406` getStudentAiStyle 用 findFirst 应为 findUnique（有唯一约束）
- **P2-10** `screening.ts:293` picked.includes O(n²) → 用 Set
- **P2-11** `db.ts:5` 生产环境不挂 globalThis，serverless 冷启动每次 new PrismaClient
- **P2-12** `db.ts:5` 未配置 log 级别，慢查询/错误无法发现
- **P2-13** `serverEnv.ts:32-48` getLLMConfig memoryConfig 为 null 时每次读文件 → 首次后缓存
- **P2-14** `realtime.ts:19` setMaxListeners(0) 无限监听器，订阅未清理会内存泄漏
- **P2-15** schema：`CompanyFeedback` 缺 `@@index([sessionId])`；`inviteCode` 既有 @unique 又有 @@index（冗余）；`Participant` 缺 `@@index([sessionId, connected])`

### 逻辑 / 规范
- **P2-16** `classroom.ts:663` getFinaleSummary avgRating 把 rating 为 null 按 0 计入分母拉低均值 → 只算有 rating 的
- **P2-17** `teacher/page.tsx:620` 导航链接用 `?code=inviteCode`，但 screen 页读的是 `?sessionId=`（参数名不匹配）
- **P2-18** 多处用数组索引 `key={i}`：`TeacherFinale:168,203`、`StudentFinale:252,307,356`、`ScreenFinale:91,94` → 用稳定值
- **P2-19** `teacher/page.tsx:796` input onChange 每次按键写 localStorage → debounce
- **P2-20** `L2StudentFlow.tsx:395,489` A05Skill/A06Try 的 useState 初始值在 process 异步加载后不更新 → 加 key 强制重挂
- **P2-21** `next.config.mjs` 缺生产配置（standalone、security headers、poweredByHeader:false）

---

## 四、问题分布统计

| 模块 | P0 | P1 | P2 | 小计 |
|------|----|----|----|----|
| lib/* 核心逻辑 | 5 | 9 | 11 | 25 |
| app/api/** 路由 | 5 | 8 | 1 | 14 |
| 页面（screen/teacher/student/L2） | 4 | 7 | 4 | 15 |
| 组件（Finale×3 + WaitingRoom） | 1 | 3 | 1 | 5 |
| schema + 配置 | 1 | 0 | 2 | 3 |
| **合计** | **16** | **27** | **19** | **62** |

> 注：部分问题跨模块（如 fetch 不检查 res.ok 在多模块出现），按最严重处归类，已去重。

---

## 五、系统性问题（根因层）

跨多个文件反复出现的根因性问题：

1. **事务缺失**：reset/end/join/generateInvitations/openFinaleRound 等多步写操作全无事务 → 并发数据不一致
2. **权限校验为零**：教师操作和学生操作均无身份校验，仅靠 anonymousId/resumeToken 弱标识，studentAnalyst 直接暴露隐私
3. **缓存失效不完整**：getSessionState 有缓存但写操作不主动失效 → 看到过期数据
4. **fetch 不检查 res.ok**：全库系统性，十余处，错误被静默吞
5. **无指数退避**：三端 SSE 固定 3s 重连，服务端不可用时无效重连风暴
6. **localStorage 无 try/catch**：隐私模式/空间不足会中断 useEffect
7. **内存级缓存/事件总线**：getSessionState 缓存、realtime 事件总线都只单实例有效

---

## 六、修复优先级建议

| 优先 | 修复项 | 投入 | 收益 |
|------|--------|------|------|
| 立即 | P0-1 连接池 1→10 | 1 行 | 慢得惊人 → 正常 |
| 立即 | P0-4 缓存失效 + P0-6 checking 卡死 + P0-5 SSE 无回调 | 小 | 功能性卡死全消 |
| 立即 | P0-2/3 事务 + P0-15 外键 | 中 | 数据一致性 |
| 短期 | P0-8/10/11/12/13 错误处理统一 | 中 | 健壮性 |
| 短期 | P1-1 finale 轮询降频 + P1-2/3 并行/缓存 | 小 | 性能 |
| 中期 | P1-9/10 SSE 退避 + 竞态保护 | 中 | 健壮性 |
| 中期 | P2-1 权限校验（正式发布前必须） | 大 | 安全 |
| 长期 | P2-7~14 查询优化 + schema 索引 | 中 | 性能 |

---

## 七、一句话总结

**最致命的是 `connection_limit=1`（慢得惊人）+ 缓存写后不失效（数据过期）+ 事务缺失（并发数据损坏）+ 三处功能性卡死（checking/SSE/EventSource）。** 性能问题修 1 行就能解决大半；但代码层面还有 62 个问题，其中数据一致性和权限校验是正式上线前必须补齐的。
