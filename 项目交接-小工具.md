# 项目交接 · 小工具（md-quick-save / 抖音视频抓取）

> **给谁看**：新开的 AI 会话 / 接手者。读完本文件 + `AGENTS.md` 即可无缝接手。
> **本文件状态**：快照式交接（点对点），接手事项办完可删，或持续更新。
> **项目定位**：个人小工具（剪贴板存 Markdown / 收件箱看板 / 聊天精修 / 抖音视频抓取），独立于教学系统。
> **新会话入口**：对 AI 说 **"做小工具"**，它按 `AGENTS.md` 启动协议自动读本文件。

---

## 本机位置速查

- 代码目录：`md-quick-save\`（根：`C:\Users\Administrator\Documents\Default Project\md-quick-save`）
- 桌面图标：「保存Markdown」「收件箱看板」
- 收件箱路径：`md-quick-save/config.json` 配置
- 代码目录：`douyin-video-tool\`（根：`C:\Users\Administrator\Documents\Default Project\douyin-video-tool`）
- 桌面图标：「抖音视频抓取」（指向 `启动-抖音视频工具.cmd`）

---

## 一、现状（已实现）

| 功能 | 说明 |
|---|---|
| 保存 Markdown | 剪贴板一键存 md → 默认存 `Documents/收件箱`，保存前自动查重（相似≥80% 弹窗：更新留底/另存/取消，旧内容存 `.versions/`） |
| 收件箱看板 | 本地看板（127.0.0.1:8091，`inbox_server.py`），时间流 + 内容搜索 + 相似文档归组 + 版本留底 + 查看/复制/打开/删除 |
| 聊天精修 | 看板任文档点「精修」进入逐轮标星页（整轮★/划词标星），自动剔除 LLM 重复冗语，输出仅含精选内容的 `_精选.md` |

---

## 二、抖音视频抓取工具（douyin-video-tool，2026-08-20 新建）

> **用途**：用户课堂要播抖音视频，原片清晰度差。不下载视频、直接播链接又受抖音防盗链限制，所以做了这个纯本地小工具：粘贴抖音链接 → 自动下载高清原片 → 本地视频库 → 点开即播（可全屏投大屏）。

### 现状（已实现）

| 功能 | 说明 |
|---|---|
| 抓取下载 | 教师端/页面粘抖音分享链接 → 调用本机 `yt-dlp`（已装 2026.7.4，含 ffmpeg）串行下载高清原片（`bv*+ba/b` 合并 mp4）到 `videos/` |
| 队列去重 | 同时只跑 1 个任务；同 URL / 同视频 id 不重复下载 |
| 视频库 | `GET /api/state` 列视频与任务；标题/大小/时长（ffprobe 取）/mtime |
| 点开即播 | `<video>` 支持 **HTTP Range**（可拖进度），播放器全屏按钮投大屏 |
| 删除 | 列表里删除文件 + meta |

### 技术要点

- 纯 Node http server，**零 npm 依赖**，单机监听 `127.0.0.1:8120`。
- 文件：`server.js`（服务+队列+RANGE）/ `public/index.html`（单页前端，深色大字） / `启动-抖音视频工具.cmd`（双击起服+开浏览器）。
- 元数据落 `videos/meta.json`；启动时扫描 `videos/` 目录重建（缺失剔除、未登记 mp4 补录）。
- 进度解析：yt-dlp `--print title`（首行标题）+ `--progress-template`（`字节/总字节 速度`）。

### 已知注意事项

- **抖音风控**：部分视频/账号会报 `Fresh cookies are needed`，此时需给 yt-dlp 配 cookie（浏览器导出 Netscape 格式）。**真实链接真机下载尚未验收**（测试链接均因无 cookie 失败）。
- **水印**：多数视频 yt-dlp 能拿无水印版，个别受平台策略限制带水印。
- 桌面图标「抖音视频抓取」→ 双击即用。改端口要同时改 `server.js` 与 cmd。

---

## 三、待办

- **md-quick-save**：⚠️ 用户明确当前不满足要求、想继续开发，具体需求待确认后登记。
- **抖音视频抓取**：真实抖音链接真机下载验收（需用户提供一条真实分享链接）；如果抖音报 `Fresh cookies are needed` 需配 cookie。

---

## 四、怎么跑

- `md_save.pyw`（保存 Markdown）、`inbox_server.py`（收件箱看板，127.0.0.1:8091）—— md-quick-save
- `启动-抖音视频工具.cmd` 或桌面「抖音视频抓取」（douyin-video-tool → 127.0.0.1:8120）

---

## 五、接手第一步建议

1. 先读 `AGENTS.md`（目录/铁律）
2. 问用户：小工具下一步想加什么功能（不满足要求的具体点在哪）
3. 按用户需求实施
