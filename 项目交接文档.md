# 项目交接文档 · 2026-08-13

> **给谁看**：新开的 AI 会话 / 合作伙伴。读完本文件 + `AGENTS.md` + `待办清单.md` 即可无缝接手。
> **本文件状态**：快照式交接（点对点），接手事项办完可删，或持续更新。
> ✅ **2026-08-13 交接已完成**：第三节的 53 个未提交改动已全部按四批提交（见下方第三节更新）。
> 📍 **新会话第一眼看「本机位置速查」**（文件都在哪 / 怎么连服务器），接着看「最近一次交接」。

---

## 本机位置速查（2026-08-15 补充）

### 根目录
**`C:\Users\Administrator\Documents\Default Project`** —— 这是 git 仓库根目录，也是所有项目的工作根目录。

### 目录一览

| 目录 | 是什么 | 状态 |
|---|---|---|
| `aliyun-classroom-v2/` | **试听课方案**（最近主战场，Next.js + Prisma） | ✅ 在跑，线上 8.130.70.114:3000 |
| `ar-class-monitor-demo/` | 正式课教学监控系统（Python，本机 8099） | ✅ 在跑，近期未大动 |
| `md-quick-save/` | 个人小工具（剪贴板存 Markdown） | ✅ 独立 |
| `skills/` | 可复用 Skills（教案生成等） | 存档 |
| `正式课文档/` `试听课文档/` | 课程文档 | 常新 |
| `_backup_finale_20260813/` | 旧收官代码备份 | 存档 |

### 试听课项目关键文件（主战场）

- 代码目录：`aliyun-classroom-v2\src\`
- **收官三个大屏 HTML**（都在 `aliyun-classroom-v2\public\`）：
  - `pain-wall.html`（痛点墙）
  - `four-wings.html`（四翼展示）
  - `price-reveal.html`（价格颁布）
- 部署配置：`aliyun-classroom-v2\.env.deploy`

### 服务器与部署

- **线上地址**：`http://8.130.70.114:3000`
- **SSH**：`C:\Windows\System32\OpenSSH\ssh.exe` 连 `root@8.130.70.114`，密钥在 `C:\Users\Administrator\.ssh\id_ed25519`（已配好，免密）
- **部署流程**：本地打包 → `scp` 到 `/root/classroom-v2.tar.gz` → 服务器 `bash /root/classroom-v2/update.sh`（详见下方第四节）
- **服务器代码目录**：`/root/classroom-v2/`

---

## 〇、最近一次交接（2026-08-15 晚，置顶）

**本次会话做完了两件事：**

1. **A2 纯展示页改成内容页**（铁律，commit 657dacf）：发布任务/产生疑问/找到方法/认知思考/未来展开/最后升华 6 个纯展示环节从功能页改成**内容页**（`pages.ts` 新增 `CONTENT_SEEDS`，seed 成 kind=content，标题+内容块存 MediaItem slot=page:{id}）；有互动的（会前准备/AI团队开会/检验提交/梦想墙）保持功能页。

2. **收官模块重建**（commit 0fe8d91 / dac0602 等）：试听课 `aliyun-classroom-v2` 新增 `CLOSING` 模块（type=closing，模板升 **A-v11**），用**环节机制**（和 A0/A1/A2/A3 完全一致，页面序列点环节投屏），三个环节页：
   - **痛点墙**（`closing:pain`，`public/pain-wall.html`）：AI坑↔自己的坑 逐张点亮（8 步），子控制条上一张/下一张
   - **四翼展示**（`closing:wings`，`public/four-wings.html`）：创造/驾驭/成长/传播 + 成长链（6 步）
   - **价格颁布**（`closing:price`，`public/price-reveal.html`）：值多少→¥39 线上小课→¥3,300-9,800 训练营（多针对行业痛点不适合小白）→转折→¥??? 悬念→¥299 白色大数字→三组价值+金色CTA（7 步）

   **关键机制**：三个 HTML 都放 `public/`，大屏 `screen/page.tsx` 按 subState 前缀渲染（`closing:pain*`/`closing:wings*`/`closing:price*`），教师端 `AvatarTeacher` 页面序列点环节投屏，每环节有子控制条（`PainWallTeacherBar`/`FourWingsTeacherBar`/`PriceRevealTeacherBar`）；HTML 内加 `window.addEventListener('message')` 接收 postMessage 控制 step。

**已部署 8.130.70.114:3000 并验证**（三端 200、CLOSING 页面序列 3 页、环节 subState 切换正常）。

**下一步（接手先看这里）**：
- 价格颁布页 **CTA「立即报名」目前是占位**（点击只打日志），需接真实报名链接
- 价格颁布页**配图用真实截图**（张老师要求：¥39 线上课、¥3,300-9,800 训练营的真实售卖页截图），目前是纯文字卡片、无图
- 收官**后续环节待填**：课程介绍、系统介绍（张老师定的收官顺序：痛点→四翼→价格→之后还要介绍课程和系统）
- 收拢单一数据源、页面序列"上一条/下一条"导航（长期待办，见 `待办清单.md`）

**部署方法**（本次实战验证过）：本地打包（排除 node_modules/.next/.git/.env.local/public/media）→ `scp` 到 `root@8.130.70.114:/root/classroom-v2.tar.gz` → 服务器 `bash /root/classroom-v2/update.sh`。SSH 用 `C:\Windows\System32\OpenSSH\ssh.exe`，密钥 id_ed25519 已配好。

---


## 一、一句话现状

**这是一套线下 AI 授课系统的仓库，里面有两个在跑的项目**：

| 项目 | 目录 | 线上地址 | 状态 |
|---|---|---|---|
| 试听课方案（最近主战场） | `aliyun-classroom-v2/` | http://8.130.70.114:3000 | ✅ 在跑，本周天天在改 |
| 正式课教学监控系统 | `ar-class-monitor-demo/` | 本机 8099（`python server.py 8099`） | ✅ 在跑，近期未大动 |

> ⚠️ **最重要的一件事：仓库里有大量"已部署到服务器、但本地 git 未提交"的改动**（见第三节）。接手第一件事先搞清楚哪些该提交。

---

## 二、最近在干什么（本会话主线）

**试听课 A0 开场按新流程改造，已完成并部署**。来源是顾问建议，见 `试听课文档/A0流程修改对照表.md`：

1. 开场·手指图（`a0:intro1`）→ 2. 开场·二维发展图（`a0:intro2`）→ 3. 三问（不透露关系测试）→ 4. 系统判定 → 5. 揭晓结果 → 6. 三种形态 → 7. 六步滑块（发现器）→ 8. 工具/伙伴两图 → 9. 镜子"我们在哪儿"（`a0:mirror`）→ 10. 收束"已经来了"（`a0:closing`）+ A1。

教师端 A0 步骤已改为这 10 步，大屏/学生端/教师端三端同步。

**最后一步（尚未提交）**：按用户要求改 `aliyun-classroom-v2/public/ai-timeline.html`（A0 的二维发展图）：
- 去掉图上/下方的所有文字（大标题、引导语）
- 保留图表本身：横坐标年份、纵坐标层级、柱子里的文字
- 图放到最大（大屏用）
- 图正上方加了标题 **"AI 这几年，帮人做成了多少事"**

已部署验证（`/ai-timeline.html` 返回 200）。**这个文件改动未 git 提交。**

---

## 三、⚠️ 未提交的改动（接手重点）~~✅ 已全部提交（2026-08-13）~~

`git status` 原 **53 个未提交项**，已按四批提交完毕：

1. **正式课监控系统**（commit 453d06c）：教师端课堂任务区改版（生成课堂→已加载课程卡片绿/红解锁）+ 大屏内容块隐藏管理（`/api/screen?all=1` + `/api/screen/hide`）
2. **ai-timeline.html**（commit acbc87f）：二维发展图去掉上下文字、图放大到最大，正上方加标题「AI 这几年，帮人做成了多少事」
3. **试听课 v2 大改造**（commit e76fa97）：A1数字分身/P2快速入门网站/P3数字生命共生缸 + A0六步滑块发现器 + 内容框架 + 媒体库增强 + 旧收官备份 `_backup_finale_20260813/` + 素材图与视频
4. **文档**（commit 6ea1302）：`正式课方案二三.md`→`试听课方案二三.md` 改名、现场话术补全、新增 A0对照表/开场情绪过渡/试听课脚本/课程2/图片 + `顷悟通用偏好引导规则.md`

> 仅剩 `试听课文档/ses_0192535e0ffe6ZZW7BtPSXZdCS.json`（40MB）未跟踪：是用户之前的对话存档，**留原处不动、不提交**。

---

## 四、怎么跑 / 怎么部署

### 试听课 `aliyun-classroom-v2`（Next.js + Prisma）
```
本地开发：npm run dev
构建：npm run build
部署到 8.130.70.114：打包 → scp 到 /root/classroom-v2.tar.gz → 服务器解压 → bash /root/classroom-v2/update.sh
  （update.sh：解压→docker build→rm 旧容器→run，带 -v /root/classroom-media:/app/public/media 挂载→prisma db push→验证）
```
⚠️ 部署铁律（吃过亏）：
- **容器重建会丢容器内 public/media 上传文件** → 上传文件必须持久化到宿主机 + 走动态路由 `/api/media/file/<name>`，`next start` 不实时服务运行时新增的 public 文件
- 改 public 静态文件（如 ai-timeline.html）需要重新构建部署才生效（`next start` 不实时读）

### 正式课 `ar-class-monitor-demo`
```
python server.py 8099
```
页面：学生 `/student/`、教师 `/monitor/`、大屏 `/bigscreen/`。架构详见 `AGENTS.md`。

---

## 五、待办清单（摘自 `待办清单.md`，详见原文件）

- **挂起·等张老师拍板**：判断skill知识库验收、判断skill技术路线（A/B）、三项目映射方案验收
- **进行中**：投大屏vs推学生端是否联动、课程库上云（阿里云OSS，⚠️重要必做）
- **试听课入口简化+产品化**：首页3端入口/教师端3个快捷链接/手动创建课堂，张老师觉得繁琐，暂不动。未来方向=准入机制+易用+可产品化（登记待办，勿做）
- **试听课 A0**：二维发展图已改为本仓库 `ai-timeline.html`（本会话刚做，未提交）；待办=张老师若要自制图再替换、P2 里程碑内容填充
- **试听课页面序列框架（PPT式）**：✅ 已落地第一版（2026-08-13）：`LessonPage` 表 + 教师端页卡片列表（插入/排序/隐藏/改标题/删除）+ 大屏逐页渲染 + 内容页支持文字/图片/视频/链接/网页。详见 `待办清单.md` 那条。

## 六、⚠️ 服务器磁盘满事件（2026-08-13，已根治）

**现象**：点「创建课堂」报 500，F12 显示 `api/classroom/latest` 500，错误 `Can't reach database server at 127.0.0.1:5432`。
**根因**：服务器 40G 磁盘被 Docker 悬空镜像塞满（每次 `update.sh` 构建不清理，累积 135 个镜像占 26.8GB）→ postgres 写不进临时文件 → 崩溃。
**已根治三道保险**：
1. `update.sh` 构建后自动 `docker image prune -f`
2. 服务器每日 4:00 cron：`docker image prune -af --filter until=168h`（日志 `/var/log/docker-prune.log`）
3. 本次已清理 20.67GB，磁盘回 47%
**再遇同样问题**：`df -h /` → `docker system df` → `docker image prune -af` → `systemctl start postgresql`。
**教训**：以后部署必须用 `update.sh`（含清理），不要手动裸 `docker build`。

## 七、接手第一步建议

1. ~~先读 `AGENTS.md`（目录/架构/铁律）→ `待办清单.md`（全部待办）→ 本文件~~ ✅ 已读
2. ~~处理未提交改动：`git status` 逐个确认，分类提交~~ ✅ 已完成（见第三节）
3. 问张老师下一步做什么（本会话已做：页面序列框架落地 → 待张老师试玩反馈）

---

## 七、关键文件速查

| 文件 | 作用 |
|---|---|
| `AGENTS.md` / `文档索引.md` / `待办清单.md` | 项目三件套（铁律：改架构/功能后必须同步这三份） |
| `试听课文档/A0流程修改对照表.md` | A0 新流程依据（顾问建议） |
| `试听课文档/A0开场流程蓝本.md` | A0 逐页流程 + 素材缺口 |
| `试听课文档/开场情绪与环节过渡内容.md` | A0/A1/A2/A3 后放什么 + 收官弹药链接 |
| `aliyun-classroom-v2/src/features/avatarLesson/config.ts` | A0_INTRO / A0_QUESTIONS_GUIDE / artImages 配置 |
| `aliyun-classroom-v2/src/components/AvatarA0Screen.tsx` | 大屏 A0 各页渲染 |
| `aliyun-classroom-v2/public/ai-timeline.html` | 二维发展图（本会话最后改动，未提交） |
| `ar-class-monitor-demo/` | 正式课监控系统（详见 AGENTS.md） |
