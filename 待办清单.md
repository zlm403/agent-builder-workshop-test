# 待办清单 · 项目事项总账

> **用法**：时不时翻这张表。办完的勾掉（`[x]`），没办的挂在上面等。新增事项随时补。
> **维护**：AI 会话结束时，若产生新的待办，登记到这里并 git 提交。
> 与 `文档索引.md`（有哪些文档）互补：这里管"还有什么事要做"。
> **按项目分节**：A=试听课 / B=正式课 / C=小鸟课堂 / D=小工具。做哪个项目看哪节。

---

## 历史交接摘要（存档）

> ✅ **2026-08-13 会话交接已完成**：仓库 53 个未提交改动已按四批提交完毕（①正式课监控系统课堂任务区改版+内容块隐藏 ②ai-timeline.html 二维发展图 ③试听课 v2 三作品大改造+内容框架+媒体库+备份 ④试听课/正式课文档）。仅剩 `试听课文档/ses_*.json`（40MB，用户之前的对话存档）未跟踪、留原处不动。
> ✅ **2026-08-14 会话交接**：A1 重构 13 屏；A2 重建（改名 A2_SITE、AI 团队功能页、团队卡片交互）；**P3（养成游戏）+ 收官阶段已彻底删除**（代码+数据库表），完整内容保留在 `试听课文档/P3与收官内容全览.md` 供重建参考；大屏删框架；位置调整框架。**下一步**：①A2 纯展示页改内容页 ②A3 重建 ③收官重建 ④收拢单一数据源 ⑤页面序列上一条/下一条。
> ✅ **2026-08-15 会话交接**：①A2 纯展示页改成内容页已完成（commit 657dacf）。**下一步**：A3 重建 → 收官重建 → 收拢单一数据源 → 页面序列上一条/下一条。
> ✅ **2026-08-15（同日晚）会话交接**：**收官模块重建已完成**（commit 0fe8d91/dac0602 等，模板升 A-v11）——痛点墙（pain-wall.html）/ 四翼展示（four-wings.html）/ 价格颁布（price-reveal.html），环节机制 + 教师端子控制条，已部署 8.130.70.114 验证。**下一步**：收官后续环节待填 → 价格页 CTA 接真实报名链接 → 配图换真实截图。
> ✅ **2026-08-18 会话交接**（详见 `项目交接-试听课.md`）：**教师端双屏预览缩放修复**（transform:scale + maxWidth，commit 2957700；大屏 600 / 学生端 380，已部署验证）；**《我的世界》本地调试指南**（commit 19339b6）。**待补**：①《我的世界》文档滞后（AGENTS.md 七章仍写"文档待落地"，实际已落地）需同步三份文档 ② 未提交改动待处理（见 `项目交接-试听课.md` 第三节）。
> ✅ **2026-08-18 交接文档体系改造**：`项目交接文档.md` 改为**总索引**；按项目拆出 `项目交接-试听课.md` / `项目交接-正式课.md` / `项目交接-小鸟课堂.md`（C:\classroom-v3）/ `项目交接-小工具.md`；AGENTS.md 加「新会话启动协议 + 项目登记表」；本表按项目分节。

---

# A. 试听课项目（aliyun-classroom-v2）

## A1. 挂起 · 等张老师拍板

- [ ] **试听课入口简化 + 产品化**（2026-08-13 讨论，先不搞，登记待办）—— 首页 3 个端入口卡片、教师端初始页「教师端/大屏幕/学生端」3 个快捷链接、手动「创建课堂」按钮，张老师觉得繁琐且困惑。**暂不动**。未来方向：① 准入机制（需登录/密钥）；② 方便别人用；③ 可能产品化售卖。（来源：2026-08-13）

## A2. 进行中 / 未打通

- [ ] **云端登录 + 本地视频播放（2026-08-24 已打通）** —— ✅ 目标达成：`https://storyseed.com.cn/lesson2` 已上线（HTTPS 安全上下文 → PNA 放行 → 本地 9123 秒开）。**完整排查链**（2026-08-23→24）：①video-server.js 监听 `::`→改 `0.0.0.0`（`29e9c52`）；②GreenHub 退出后系统代理残留 `ProxyEnable=1` 指向死端口，浏览器访问局域网被劫持 → 关代理；③真正卡点 = **Chrome PNA**：HTTP 公网页面（8.130.70.114:3000）请求局域网资源被拦，本地源其实一直在发请求但被拦后自动回退云端兜底。**解决方案 = 上 HTTPS**：部署 `/lesson2` basePath 版（commit `a9d0dd5`）+ reading-app 的 Caddy 加 `handle /lesson2* → 172.17.0.1:3000`（`a9d0dd5` 部署 tag，四端 200）。**教训**：①Dockerfile 必须 `COPY next.config.mjs`，否则 `next start` 找不到 config、basePath 失效（2026-08-24 部署失败两次根因）；②next.config 的 basePath 读 `process.env.NEXT_PUBLIC_BASE_PATH`，运行时容器 env 必须有该变量（已加进服务器 .env/.env.deploy）；③Caddyfile 改后需重启容器才生效。**新入口**：`https://storyseed.com.cn/lesson2`（学生/大屏/教师零配置）。**待办**：真机验证（手机/学生电脑浏览器打开 lesson2 播 A1 视频秒开）。（来源：2026-08-23，2026-08-24 打通）
- [ ] **教师端预览小屏视频不播放只显示提示（preview=1）** —— ✅ 已完成（2026-08-20，commit `9e816ec`）。教师端双屏预览 iframe src 各带 `&preview=1`（`(teacher)/teacher/page.tsx`：大屏 `/screen?sessionId=..&preview=1`、学生端 `/student?code=..&preview=1`）；URL 带 preview=1 时 `VideoSource.tsx`/`A1RealityVideo.tsx`/`SmartVideo.tsx` 一律不创建 `<video>`、不预加载、不播放，只渲染占位提示「🎬 视频在真大屏播放中」（新增 `src/lib/preview-mode.ts` + `src/components/VideoPreviewPlaceholder.tsx`，双阶段渲染防 hydration mismatch，组件内所有 useEffect 带 preview 守卫）。不带 preview=1（真大屏/正常学生端）行为完全不变。已 tsc 无错误 + build 成功 + 本地生产 3001 四 URL 全 200。**✅ 已部署线上（2026-08-20 20:48）**：commit `9e816ec` → tag 镜像 `classroom-v2:9e816ec`，deploy.sh 走锁，容器内 BUILD_ID=`wS5SzUyyVf7JvSQj8--wK` 一致；《我的世界》4 源码文件容器内 hash=线上原版、`data/world` 时间戳未变（包排除 `data/` 生效）。（来源：2026-08-20）
- [ ] **《我的世界》模块（试听课 A3 环节，模块 `A3_WORLD`）** —— ✅ **表现规格引擎已落地（2026-08-25，5 提交：cdebe50→1ce08f8→dfa620c→5ce093d→62b0649）**。**实际架构（已修正早期文档的"独立 world-engine 进程连 PostgreSQL"说法）**：JSON 文件存取（`data/world/world-*.json`，`store.ts` 原子写）+ 纯函数引擎惰性推进（`engine.ts`，`GET /api/world` 按墙钟差补算 tick，提交即发布）。**本次落地内容**：①`src/lib/world/spec.ts` 新建 LifeSpec 类型（onMeet/onHit/onResource/onWave/onGrow/onDeath/mood→动作数组）+ 能力库枚举 + merge/normalize/ruleSpec 规则回退；②引擎 `tick` 抛结构化事件（`KeyEvent.type`：enter/meet/help/resource/sleep/wake + targetId，`emitEvent` 节流防刷屏）；③`api/a3/llm` extract 改造为六块逐块生成规格片段（能力库枚举，禁止 AI 发明新原语，followup 澄清，失败规则回退不 502）；④学生端 `WorldStudent.tsx` 新增六块设计面板（逐块对话→生成片段→合并提交带 `shape+spec`）；⑤大屏 `WorldScreen.tsx` 通用执行器 `runSpec` 读 spec 调能力库播放（emitSelf 草图粒子/lightLink 光带/scale/dim/glow/jitter/bubble/cry/dance/fade/orbit/nuzzle，未知动作回退文字标签），替代旧 `fxForEvent` 文本匹配。**已验收**：tsc 零错误 + build 成功 + 本地 3001 跑通（world/life/extract 六块/student/screen 全 200）。**待做**：真机验证（学生端六块设计→大屏看表现）→ 部署线上（走 deploy.sh）→ A组关系反馈 → 6次A/B实验 → 画面打磨。（来源：2026-08-15，2026-08-18 核实，2026-08-25 表现规格落地）
- [ ] **上机视频笔记本本地化 + 换机迁移** —— ✅ 已完成（2026-08-20，commit `4a1e930`）。视频源「笔记本优先、云上兜底」三级回退：教师端右上角运行时配「视频服务器地址」（存 `data/video-server.txt`，API `/api/video-server`，无需重新构建）；前端统一 `src/lib/video-src.ts`（getVideoBase 每次拉取不缓存）+ `SmartVideo.tsx`；`VideoSource.tsx`/`A1RealityVideo.tsx`/`MediaPlayer.tsx`/`ContentSlot.tsx`/`ContentPage.tsx` 全部走 base 优先 → `/videos/` → `/api/media/file/`。上机笔记本跑 `npm run video-server`（`scripts/video-server.js`，9123 端口，纯 Node 无依赖，支持 Range）服务 `public/videos/`（2 个 mp4）；换机四步 + 回退 + FAQ 见 `aliyun-classroom-v2/docs/上机视频本地化与换机迁移.md`。不填地址即走公网自动回退。（来源：2026-08-20）
- [ ] **A1 现实视频任务已回滚（2026-08-20）** —— 原「视频只在真大屏播放+控制条移教师端+faststart」改动（commit d1d7400/06c8a35/e910225）经用户确认整体回滚，代码恢复 `1ebb2e1` 原始版（进 c9 自动播放 + 页内控制条），已重新构建并部署线上（tag `rollback-1ebb2e1`，四端 200 验证通过，deploy.sh 走锁）。⚠️ 注意：线上回滚后 `a1-reality.mp4` 也恢复为**重编码前**（moov 在尾部的 88917626 字节）版本，若再遇卡顿需重新 faststart。
- [ ] **收官后续环节待填** —— 课程介绍、系统介绍（张老师定的收官顺序：痛点→四翼→价格→之后还要介绍课程和系统）。（来源：2026-08-15）
- [ ] **价格颁布页 CTA 接真实报名链接** —— 目前是占位（点击只打日志）。（来源：2026-08-15）
- [ ] **价格颁布页配图换真实截图** —— ¥39 线上课、¥3,300-9,800 训练营的真实售卖页截图，目前纯文字卡片无图。（来源：2026-08-15）
- [ ] **收拢单一数据源** —— 长期待办。（来源：2026-08-14）
- [ ] **页面序列"上一条/下一条"导航** —— 长期待办。（来源：2026-08-14）
- [ ] **试听课 A0 开场按新流程** —— ✅ 已完成改造（2026-08-13/14）：开场手指图→二维发展图→三问→揭晓→三种形态→工具/伙伴两图→镜子→收束→A1。**待办**：二维发展图替换为张老师自制图、P2 里程碑内容填充。（来源：2026-08-12）
- [ ] **试听课三作品改造**（2026-08-12，另一项目，与正式课无关）—— ✅ A0 章节化；✅ A1 十七环节（数字分身）；✅ P2 六座山十二阶段（快速入门网站）；✅ P3 数字生命共生缸（十阶段）。内容框架已上线。**待办**：A1→P2→P3→收尾过渡话术内容填充、P3 共生缸大屏效果真机验证、收官 closing 接入试听课课程。旧收官代码已备份 `_backup_finale_20260813/`。
- [ ] **试听课教师端·环节内容块管理 + 媒体库增强** —— ✅ 已完成（2026-08-13）：内容块 hidden 字段、就近隐藏/删除/增加、重置本环节、去重复按钮。已部署验证。
- [ ] **试听课环节改造成"页面序列"框架（PPT 式）** —— ✅ 已落地第一版（2026-08-13）：`LessonPage` 表 + 教师端页卡片列表 + 大屏逐页渲染 + 内容页支持文字/图片/视频/链接/网页。**修复**：删除根目录重复 `schema.prisma`（Prisma CLI 读旧 schema 的坑）。**后续可选**：媒体库全局入口暂不支持 embed 网页类型。
- [ ] **A2 纯展示页改成内容页（铁律）** —— ✅ 已完成（2026-08-15，commit 657dacf）。张老师定铁律：但凡不是"学生手机屏/大屏互动"的页面（只是显示文字/图/视频），都必须做成**内容页**。A2 里 6 个纯展示环节已改。A3、收官重建时也照此铁律。
- [ ] **媒体库插入位置动态化（跟最新章节走）** —— ✅ 已完成（2026-08-21，commit `0ed9922`）。根因：媒体库下拉原读 `slots.ts` 静态死清单，与 pages.ts 真实页面序列脱节（旧分组「数字分身/快速入门网站」、死 slot `a1_c1~c6_after`/`a2_s1~s3/s7/s9/s10_after` 等，大屏早已不渲染）。改造：①`slots.ts` 分组对齐 `PAGE_GROUPS`、`CONTENT_SLOTS` 收敛为**大屏组件真实渲染**的命名 slot（逐组件核对 A0/A1/A2）；②新增 `GET /api/pages/slots` 返回每组「命名 slot + 内容页 page:{id}」，内容页永远跟随最新章节（增删页面下拉自动更新）；③`MediaManager` 下拉改为拉该 API；④`SlotContentManager` 分组映射对齐。第 8 页收束视频一并改走媒体库 `a0_closing_video` 插槽（移除硬编码按钮+内嵌视频）。已 tsc + build + 本地 3001 验证（A1 组 = 7 命名 + 8 内容页，四端 200）。**待部署线上**。
- [ ] **A2 页面序列改造 + 上下箭头排序修复（2026-08-22，commit `94f1c01`）** —— ✅ 本地已提交。①**钩子开场 a2:hook 从功能页转内容页**（seed 5 个默认文字块），现在和其他纯展示页一样能放 HTML（脚本/界面都可）；②**修复上下箭头排序被覆盖的 bug**：根因 = `ensurePages()` 每次加载都把种子页强制重排回代码写死的 fullOrder，把教师用箭头调的顺序拉回；现改为仅新增/删除种子页时才重排，平时保留教师排序；③**交互页 s4/s5/s6 大屏补内容槽**（`a2_s5_after`/`a2_s6_after` 图片与视频分支原无内容槽），现在大屏图/视频下方也能放内容块（含网页 HTML）；④清理 `a2_hook_after` 死槽。已 tsc + build + 本地 3002 验证：A2 12 页无重复、钩子转 content 成功。**待部署线上**（开发阶段不上传，见 2026-08-22 用户定铁律）。
- [ ] **内容页一键替换为 HTML（A2 换内容用，2026-08-22，commit `1b6a60c`）** —— ✅ 代码完成 + 本地验证，**本地 git 已提交**（commit `1b6a60c`）。⚠️ **按用户要求：开发阶段不上传服务器，等全部做好再统一部署**。给"环节页整页放 HTML"补上了缺失能力，两种方式任选：
  1. **一键脚本**：`node scripts/replace-page-html.js <group:refKey> <html文件路径> [标题]`（例 `a2:s1`）→ 拷 HTML 进 `public/media/` + 清空该内容页全部旧内容块 + 插入「网页」块走 `/api/media/file/` 动态路由（无需重启即生效）。refKey 见脚本注释（A2 内容页 `a2:s1/s2/s3/s7/s9/s10`、A1 `avatar:c1~c6/html07/c11`）；只替换 kind=content 内容页，内置功能页不可替换。
  2. **界面**：「编辑本页」新增「🗑 清空本页」按钮（DELETE `/api/media?slot=page:{id}` 批量删）+「网页」类型可**上传 HTML 文件**；媒体库全局下拉加「网页」类型 + 上传 HTML。上传接口/文件路由已放行 .html 且 MIME=text/html。
  - 验证：tsc + build 通过；实测 a2:s1 替换→恢复（seed 回写默认块正常）；批量删除接口返回 count。部署注意：`public/media/` 打包会排除，新 HTML 文件需随挂载目录 `/root/classroom-media` 上传；**打包须排除 public/videos（云端已有，勿重复传）**。
- [x] **A2 大屏换用户自制 HTML（2026-08-22，commit `e899a29`）** —— ✅ **已部署线上（2026-08-22 22:00）**，tag 镜像 `classroom-v2:e899a29`，deploy.sh 走锁成功，四端 200。①**s4/s5/s6 大屏整页嵌用户 HTML**：`A2_STAGES` 配 `media:'embed'`+`/api/media/file/a2-sN.html`，大屏 iframe 整页显示用户的 S4/S5/S6.html；**功能页方案而非转内容页**（转内容页会让学生端收到 `page:` 信号一律显示"请看大屏"，s6 的"提交作品"按钮是学生上墙唯一入口会消失——学生端交互必须保留）。②**A2 页面序列重排**（最终顺序：钩子→产生疑问→找到方法→会前准备→AI团队开会→自动执行→检验提交→作品墙[功能页保留]→三种认知[新增]→未来展开→梦想墙→最后升华；发布任务 s1 隐藏；梦想墙/最后升华调换）。③内容页换 HTML：钩子(S0)/产生疑问(S2)/找到方法(S3)/检验提交(S7)/最后升华(S10) 换用户内嵌版；三种认知(S8) 新增内容页。④清理重复"钩子开场"页。
  - **部署附带完成（根治拔河）**：11 个 A2 媒体文件已上传服务器挂载目录 `/root/classroom-media`（9 个 a2-*.html + 2 个 mp4），`/api/media/file/` 全 200；DB 清理重复 `a2:hook` 旧文本版（保留 HTML 版 931281cd）+ 手动对齐 A2 seq（s7 在 wall 前、s9 在 s8 前、s1 隐藏置尾）。**拔河已根治**：新代码 `BUILTIN_SEEDS` 无 a2:hook、ensurePages 不再重建重复页。
  - ⚠️ **教训**：`ensurePages` 只清理 builtin 孤立页，**不清理重复 content 页**，且重排仅在种子页增删时触发——所以部署后需手动清重复 content 页 + 对齐 seq（本次已做）。
  - **待办**：①**未来展开页放视频 —— ✅ 已完成（2026-08-22）**：张老师选 `试听课文档/视频/8月15日/8月15日.mp4`，安全名 `a2-s9-20260815.mp4` 上传挂载目录（本地也同步 `public/media/`），未来展开页（a2:s9）占位 text 换成 video 块（`/api/media/file/a2-s9-20260815.mp4`，SmartVideo 三级回退），线上 200 即点即播。②**作品墙 S9 融合 —— ✅ 已完成（2026-08-22，commit `a4a765e`，已部署 tag `a4a765e`）**：张老师 `A2-S9.html` 降落动画 + 系统真实数据；**核心断链修复**（`saveSiteCode` 原无调用 → 网站 HTML 从不落库 → 作品墙永远空）；学生提交时给网站起名（`a2_site_entry` 加 `title` 列，db push 已上线）；大屏作品墙改 S9 风格（`a2land` 逐卡降落 + 真实 iframe/作品名/AI 团队，analytics 扩展 `items[]`）。线上真机验证：DEMO1 全链路（chat→HTML 落库→提交带标题→analytics item）通过，测试数据已清理。③**学生端 A2 交互重新设计**（张老师定：初始只看大屏→S4 推学生端操作→作品上墙后回看大屏→梦想墙填表→升华不管学生端）——⚠️ **梦想墙填表待做**：s8 当前还在 `isWatchOnly`（学生端只显示"请看大屏"），需让学生端能输入想法上墙（复用 `/api/classroom/{sessionId}/thoughts`）。④**A2 金黄大标题去除 + seq 冲突修复 —— ✅ 已完成（2026-08-23，commit `dbe1eb1`，已部署 tag `dbe1eb1`）**：content 页大屏顶部金黄渐变标题（ContentPage 渲染 `lesson_page.title`）用户要求去掉——`ContentPageHost` 对 A2 组 content 页传 title=null，教师端 `pageLabel` 用 A2_CN 兜底（卡片名不消失）；DB 修复"三种认知"与"未来展开"同 seq=8 的冲突（改为未来展开=8、三种认知=9、梦想墙=10、最后升华=11、发布任务=12）。

## A3. 部署相关

- [ ] **部署加固（2026-08-15 夜事故）** —— 当晚 3 次全站 404（容器内 build 残缺 .next 缺 server/app，多会话并发触发）。服务器已用本地完整 .next 覆盖恢复，全 200。**待做**：见根目录 `交接-部署加固任务清单-20260815.md`——①备份（数据库/配置/world）②提取三次故障证据③写 deploy.sh（flock 锁+审计+产物校验+唯一镜像标签+健康检查回滚）④查明容器内 build 残缺根因⑤规则已写入 AGENTS.md 六章。**规则**：部署只能走 deploy.sh 唯一入口，禁止直接改容器，必须带 DEPLOY_OPERATOR+DEPLOY_REASON。（来源：2026-08-15）

---

# B. 正式课项目（ar-class-monitor-demo）

## B1. 挂起 · 等张老师拍板（最优先看这里）

- [ ] **《判断skill知识库.md》张老师验收** —— 已扩展为三合一（判断信号+提醒话术A+分析话术B），需张老师确认判断信号准不准、话术合不合口。（来源：2026-08-12 讨论）
- [ ] **定判断 skill 的技术路线** —— A：在本系统加"分析服务"（定时扫 events.jsonl 调 AI）；B：顷悟侧做 skill。张老师二选一。（来源：2026-08-12 讨论）
- [ ] **三项目映射方案张老师验收** —— 已按《课程方案.md》三项目改造三端（学生/教师/大屏）+ 新建《上课执行方案.md》，学生作品发布链接格式待确认。（在 `正式课文档/`，来源：2026-08-12 执行）

## B2. 进行中 / 未打通

- [ ] **"投大屏 vs 推学生端"是否联动** —— 现在两者独立：大屏投屏只改大屏，学生端任务区只受"设为当前"控制。张老师未定：A保持分开 / B投大屏时也顺带推给学生端。（来源：2026-08-12 框架重构）
- [ ] **课程库上云（阿里云 OSS）** —— ⚠️ 重要必做。课程库（源文件+任务+大屏内容）现在存本机 `agent-live/library/`，**正式部署必须迁到云端**（阿里云 OSS 或自研顷悟平台），才能多机稳定访问、课程跨电脑复用。目前先本地跑通流程。（来源：2026-08-12 张老师强调）
- [ ] **顷悟 Agent 真实对话 → 事件流** —— ✅ **已打通并真机验证**（2026-08-12）：`qingwu-dialog-bridge.py` 常驻转发桥读顷悟 `memory/runtime/events.jsonl`，聊天面板 `user_message/assistant_done` 增量+去重转发到 `/api/collect`，真机验证收到学生原话且不重复。多机部署：每台学生电脑各跑一个，`--server` 指向老师机。
- [ ] **判断 skill 本身** —— 知识库已扩展为三合一（`判断skill知识库.md`）+ 顷悟提醒知识库已建；`water-analyzer.py` 常驻分析服务已接 **DeepSeek AI 深度解析**（规则算水位+AI生成分析，AI失败回退规则）。**待定**：顷悟应用挂载提醒（需顷悟平台侧 system_prompt/后台，通用偏好软记忆不生效，2026-08-12 已试无效后暂缓）。

---

# C. 小鸟课堂项目（C:\classroom-v3，独立 git）

- [x] **§2.7C 全链路租户隔离收口** —— ✅ 已完成（2026-08-18）：baseline + enforce-tenant-nonnull 两阶段迁移；6 处 SET NULL→RESTRICT + tenantId 非空 + CapabilityData 复合唯一含 tenantId；AuditLog/workspaceId 保持可空；paused 状态机闭环一并完成。已提交（4952d75/f59056a/567fc6e）。
- [x] **screen 正式签发链路（方案 A）** —— ✅ 已完成（2026-08-18）：教师端签发短期 ScreenCredential（增 audience+issuedAt，不加 jti，15 分钟）；新增 `/api/classroom/[id]/screen-credential` 签发端点；realtime 补 sessionId+tenantId 绑定 + ended 409 + 脱敏；state 凭据绑定 sessionId + 无凭据过渡生产默认关闭；screen 页读 credential；教师端「生成大屏链接」按钮。单元 29/29 + E2E 28/28 + 全量回归通过。**待办：本批改动未提交**。
- [ ] **提交 screen 本批改动** —— 按逻辑单元提交（screen.ts 重构 / 签发端点 / realtime+state 校验 / 前端 screen+教师端 / 测试）。详见 `C:\classroom-v3\项目交接-小鸟课堂.md` 置顶。
- [x] **教师端登录 UI（2.9）** —— ✅ 已完成（2026-08-18）：新增 `TeacherAuth` 组件（登录表单+身份展示+登出，登出走双重提交 CSRF），挂教师端页面顶部；登录后可生成大屏链接。全链路验证通过（登录→签发→大屏 state→SSE）。已提交（46643e5）。
- [x] **2.10 修复收口** —— ✅ 已完成（2026-08-18）：真实浏览器验证发现 2 个产品缺陷并修复——①缺陷 B：投影 stage 与前端校验契约冲突（`validateStage` 按 role 选投影 schema，原始配置仍用完整 schema）②缺陷 A：SSE 无限重连（ref 稳定回调身份）。新增 Playwright 浏览器 E2E（`scripts/test-screen-flow-browser.ts`）+ playwright 依赖。浏览器 6/6 通过。已提交（1b406ca/7864a72/dc086ea/5aeff07）。
- [x] **2.11 删除无凭据过渡分支** —— ✅ 已完成（2026-08-18）：删除 `isScreenTransitionAllowed` + `SCREEN_TRANSITION_ALLOW_PRODUCTION` + state route screen 无凭据过渡读取；所有环境 `state?role=screen` 无凭据一律 401；正式 credential 链路独立运行。回归全绿 + 浏览器 6/6。已提交（4a68785/6f17124）。遗留 §2.4/§2.5 脚本（test-e2e-three-roles / test-recovery-sync / test-formal-pages / test-formal-pages-e2e / test-e2e-realtime）仍含旧过渡断言，待统一清理。
- [x] **2.11a 清理遗留脚本** —— ✅ 已完成（2026-08-18）：删除 5 个 §2.4/§2.5 历史测试脚本（test-e2e-three-roles / test-recovery-sync / test-formal-pages / test-formal-pages-e2e / test-e2e-realtime），自 §2.7 起全面过时 + 依赖不存在的 `__stop_memurai` 端点、从未真跑通。覆盖点已被现有测试承接。已提交（163cc39）。
- [ ] **3.0（未安排）** —— 等 2.11 完成后再定内容。
- [ ] **可选待办** —— `listRegisteredCapabilities()` 接入诊断 UI；交接文档与 `docs/handoff/2026-08-18-handoff.md` 合并。
- [ ] **教师端 P0（2026-08-22 晚定案，待实现）** —— 用户要求先搭教师端前端 UI 试用。已产出三份文档：①`C:\classroom-v3\docs\product\teacher-end-requirements.md`（教师端完整需求清单，三阶段 + P0/P1/P2）②`C:\classroom-v3\docs\ui\teacher-wireframe.html`（线框稿，可浏览器打开看布局）③`C:\classroom-v3\docs\implementation\phase-2.8-content-base-task.md`（内容版本化 API 任务书，冻结待执行）。**P0 内容**：课程模板列表/首页入口 → 搭课工作台（章节树 + 环节三端编辑 + 保存）→ 驾驶台控制补齐（跳转/揭晓）→ `app/api/content/` 内容保存 API（LessonContent 接线）。用户已同意"先做出来试，再改"。⚠️ 派 subagent（fast/medium）多次被 abort，需在可用环境执行或主 agent 亲自做。（来源：2026-08-22）

---

# D. 小工具项目

- [ ] **continue dev（md-quick-save）** —— 用户明确：当前不满足要求，想继续开发。具体需求待确认后登记。（来源：2026-08-18）
- [x] **抖音视频抓取工具（douyin-video-tool，2026-08-20）** —— ✅ 已实现：粘贴抖音链接 → yt-dlp 下载高清原片 → 本地视频库点开即播（RANGE/全屏投大屏）。纯 Node 零依赖，127.0.0.1:8120，桌面「抖音视频抓取」图标。详细见 `项目交接-小工具.md`。
- [ ] **抖音抓取·真机验收** —— 用真实抖音分享链接实测下载成功、播放正常；若报 `Fresh cookies are needed` 则给 yt-dlp 配 cookie。（来源：2026-08-20）

---

# 其他（勿启动）

- [ ] **"回忆录"小程序** —— 另一台电脑，K8s 跑过。未来做"老人家聊天 → 生成回忆录 + 社交需求"。**未启动，勿做**。（来源：张老师口头说明，2026-08-12）

---

# 已完成（近期收口）

- [x] **收官模块重建（试听课 closing）** —— ✅ 已完成（2026-08-15，commit 0fe8d91/dac0602 等）。新增 `CLOSING` 模块（模板升 **A-v11**），环节机制 + 三个环节页（痛点墙/四翼/价格颁布）。已部署 8.130.70.114 并验证。
- [x] **教师端双屏预览缩放修复** —— ✅ 已完成（2026-08-18，commit 2957700）：transform:scale + maxWidth（大屏 600 / 学生端 380），已部署验证。
- [x] **《我的世界》本地调试指南** —— ✅ 已完成（2026-08-18，commit 19339b6）：docs/my-world/04。
- [x] 顷悟系统本体 `ar-class-monitor-demo` 复制进工作目录 + git 项目起点提交
- [x] `AGENTS.md` 建立（项目第一眼说明）
- [x] `文档索引.md` 建立（文档家底总览）
- [x] `判断skill知识库.md` 草稿建立（待校正，在 `正式课文档/`）
- [x] 确认正式课 = 三项目，以 `正式课文档/课程方案.md` 为准
- [x] 确认教学重心 = 教"顺利把项目搞上去"，技术交给 AI
- [x] 系统 8099 可访问（monitor/student/bigscreen）
- [x] 三端按三项目改造（学生/教师/大屏 tab 文案）——预备课停用为独立 tab，代码与数据保留
- [x] `上课执行方案.md` 建立（6小时三项目·老师/学生屏/大屏全流程，在 `正式课文档/`）
- [x] `任务一教案-接金币游戏.md` 建立（完整教案，在 `正式课文档/`）——后续任务二/三教案按此模板扩展
- [x] `任务二教案-心情电量.md + 覆盖矩阵` 建立（在 `正式课文档/`）
- [x] `任务三教案-内心戏.md + 覆盖矩阵` 建立（在 `正式课文档/`）——三任务全部教案+矩阵+已装进系统课程库
- [x] 目录整理：根目录只留项目说明书（AGENTS/文档索引/待办清单）；正式课文档在 `正式课文档/`，试听课文档在 `试听课文档/`
- [x] `知识点讲解手册.md` 建立（在 `正式课文档/`）
- [x] `学生自救知识库.md` 建立（在 `正式课文档/`）
- [x] **上课号签到体系**（号码即身份）落地：教师端新建课堂场次+号码池+签到状态；学生端凭上课号签到；签到写 `identity.json`；教师端按号聚合。（来源：2026-08-12，AGENTS.md 已同步）
- [x] **实时互动通道**：学生端任务看板下加"我有问题"输入框（`student_ask` 事件），教师端学生列表显示 💬 标记+问题内容。（来源：2026-08-12，AGENTS.md 已同步）
- [x] **Markdown 收件箱工具**（`md-quick-save/`，独立于教学系统）：桌面「保存Markdown」「收件箱看板」（127.0.0.1:8091）+ **聊天精修**。（来源：2026-08-13）
- [x] **服务器磁盘满根治**（2026-08-13）—— 根因：每次部署 `update.sh` 的 `docker build` 留下悬空镜像不清理，累积到 40G 磁盘满 → postgres 崩溃 → 创建课堂 500。已根治三道保险（update.sh 自动 prune + 每日 4:00 cron prune + 已清 20.67GB）。
