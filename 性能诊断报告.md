# 性能问题全面诊断报告

> 生成时间：2026-08-04
> 诊断范围：前端轮询、后端 API、数据库连接配置、Supabase 网络延迟

---

## 一、核心结论（先看这个）

**当前"慢得惊人"的真正元凶是：`connection_limit=1`。**

`.env` 里数据库连接串配置为 `connection_limit=1&pool_timeout=10`，意味着 **同一时间只允许 1 个数据库查询执行**。只要教师端、大屏、学生端有任何并发请求，剩下的全部排队等 10 秒超时，然后返回 404/500。

这不是应用层能优化掉的——是连接池容量本身被设成了 1。

---

## 二、dev.log 关键证据

### 证据 1：大量请求"正好 10 秒返回 404/500"——pool_timeout 精确触发

```
GET /api/classroom/[id]          404 in 10069ms   ← 排队 10 秒拿不到连接
GET /api/classroom/[id]          404 in 10053ms
GET /api/classroom/[id]          404 in 10047ms
GET /api/classroom/latest        500 in 10043ms   ← latest 也超时
GET /api/classroom/[id]/qrcode   500 in 10117ms
GET /api/classroom/[id]/qrcode   500 in 10062ms
```

这些 **正好 ~10050ms** 的 404/500，100% 是 `pool_timeout=10` 触发——请求排队 10 秒拿不到那唯一 1 个连接，被强制失败。

### 证据 2：学生加入花了 50 秒（排了 5 个 10 秒的队）

```
POST /api/student/join  200 in 50854ms
```

join 流程本身要 3-4 次 DB 查询，每次都要等前面排队的请求释放那 1 个连接，所以排了 ~5 轮 × 10 秒。

### 证据 3：创建课堂 36 秒

```
POST /api/classroom  200 in 36060ms
```

### 证据 4：控制操作失败还花了 20-43 秒

```
POST /api/classroom/[id]/control  400 in 43569ms
POST /api/classroom/[id]/control  400 in 20499ms
```

### 证据 5：缓存命中时 20-50ms（缓存有效，但只覆盖 getSessionState）

```
GET /api/classroom/[id]  200 in 22ms   ← 缓存命中
GET /api/classroom/[id]  200 in 38ms
GET /api/classroom/[id]  200 in 21ms
```

缓存只对 `getSessionState` 生效。其他接口（qrcode、invitations、analytics、finale、join、create）都无缓存，每次都打 DB。

---

## 三、问题清单（按优先级）

### 🔴 P0-1：`connection_limit=1` 把并发能力锁死（元凶）

| 项目 | 内容 |
|------|------|
| 位置 | `.env` 的 `DATABASE_URL` |
| 现状 | `connection_limit=1` |
| 问题 | 同一时间只允许 1 个 DB 查询，并发请求全部排队 10 秒超时 |
| 建议 | 改为 `connection_limit=10`（Supabase pgbouncer 模式下安全，单 client 不超过 60 即可） |
| 预期效果 | 并发请求不再排队，join 从 50s 降到 ~5s，创建从 36s 降到 ~5s |

> 注：之前 `classroom/latest` 每次 `new PrismaClient()` 的连接泄漏已修复，所以现在把 limit 调大不会再次打满 Supabase 配额。

### 🔴 P0-2：（finale）组件高频轮询

| 组件 | 频率 | 接口 | DB 查询数 |
|------|------|------|----------|
| `TeacherFinale.tsx:43` | **每 2.5 秒** | `/api/finale/state` | 1-3 次（getFinaleState + 可能 listCompanies） |
| `ScreenFinale.tsx:33` | **每 3 秒** | `/api/finale/state` | 同上 |
| `StudentFinale.tsx:74` | 每 3 秒 | `/api/student/...` | 1 次 |

两个 finale 组件合计每 1.4 秒就发起一次 DB 查询，持续占用那唯一 1 个连接。

| 建议 | 改为 10 秒（finale 状态变化已通过 SSE 推送，轮询只是兜底） |
|------|------|

### 🟠 P1-1：qrcode 接口每次都查 DB

| 项目 | 内容 |
|------|------|
| 位置 | `src/app/api/classroom/[id]/qrcode/route.ts:9` |
| 现状 | 每次 `prisma.classSession.findUnique`，单次 1-2 秒 |
| 问题 | 大屏每次重新渲染二维码都打一次 DB，日志里 qrcode 请求密集出现 |
| 建议 | 加 30 秒内存缓存（inviteCode 不变，二维码图也不变） |

### 🟠 P1-2：教师端 SSE 事件触发后并发发请求

| 项目 | 内容 |
|------|------|
| 位置 | `src/app/(teacher)/teacher/page.tsx:150-180` |
| 现状 | SSE 收到 `progress:update` → 同时调 `fetchInvitations`；收到 `student:joined` → 调 `fetchInvitations`；收到 `module:advanced` → 调 `fetchAnalytics` |
| 问题 | 一个 SSE 事件触发 2-3 个并发 DB 请求，叠加轮询瞬间打满连接 |
| 建议 | 这些 fetch 之间可串行化或加节流；或复用已有的 summary 数据（SSE 已推送 summary） |

### 🟡 P2-1：Supabase 东京区域跨区延迟（环境限制）

| 项目 | 内容 |
|------|------|
| 现状 | 应用跑在本地 Windows，数据库在 AWS 东京（ap-northeast-1） |
| 影响 | 单次 DB 查询网络往返 ~1-2 秒，无法通过应用层优化 |
| 缓存命中后 | 22ms（已验证缓存有效） |
| 终极方案 | 应用 + 数据库部署到同地域云（如阿里 ECS + RDS 杭州，内网 <10ms） |

### 🟡 P2-2：`getProgressSummary` 内部重复查询 session/template

| 项目 | 内容 |
|------|------|
| 现状 | `getSessionState` 查一次 session + template，`getProgressSummary` 内部又查一次 session + template（已并行化，但仍重复） |
| 建议 | `getSessionState` 把已查到的 session/template 传给 `getProgressSummary`，避免重复 |

---

## 四、当前各接口实际延迟（从 dev.log 统计）

| 接口 | 缓存命中 | 冷查询/排队 | 超时失败 |
|------|---------|------------|---------|
| `/api/classroom/[id]` (getSessionState) | 20-50ms | 10-17s | 404 @10s |
| `/api/classroom/latest` | — | 1-11s | 500 @10s |
| `/api/classroom/[id]/qrcode` | — | 1-2s | 500 @10s |
| `/api/classroom/[id]/invitations` | — | 3-13s | 500 @10s |
| `/api/analytics` | — | 4-11s | — |
| `/api/student/join` | — | **50s** | — |
| `POST /api/classroom` (创建) | — | **36s** | — |
| `/api/classroom/[id]/control` | — | 16-43s | 400 @20-43s |

---

## 五、推荐修复顺序（按投入产出比）

| 顺序 | 修复项 | 改动量 | 预期效果 |
|------|--------|--------|---------|
| 1 | `.env`：`connection_limit=1` → `10` | 1 行 | **立竿见影**，并发不再排队，join 50s→5s |
| 2 | TeacherFinale/ScreenFinale 轮询 2.5-3s → 10s | 2 行 | 减少持续占用连接 |
| 3 | qrcode 接口加 30s 缓存 | 15 行 | qrcode 从 1-2s → <10ms |
| 4 | 教师端 SSE 事件触发的 fetch 加节流/复用 | 中等 | 减少事件风暴时的并发 |
| 5 | getProgressSummary 复用上层已查数据 | 小 | 减少重复查询 |
| 6 | （长期）应用+DB 部署同地域 | 大 | 单次查询 1-2s → <10ms |

---

## 六、一句话总结

**不是云不好，是连接池被设成了 1。** 把 `connection_limit` 从 1 调到 10，配合已有的缓存和并行查询优化，本地演示就能跑顺。
