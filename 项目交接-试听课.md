# 项目交接 · 试听课系统

> **给谁看**：新开的 AI 会话 / 接手者。读完本文件 + `AGENTS.md` + `待办清单.md` 即可无缝接手。
> **本文件状态**：快照式交接（点对点），接手事项办完可删，或持续更新。
> **项目定位**：试听课方案（Next.js + Prisma），独立项目，与正式课无关。
> **新会话入口**：对 AI 说 **"做试听课"**，它按 `AGENTS.md` 启动协议自动读本文件。

---

## 〇、最近一次交接（2026-08-22，A2 大屏换用户自制 HTML 已部署上线，commit `e899a29`）

**本次会话做了一件事：把 A2 的改动（`e899a29`）部署上线，根治线上旧容器"重建重复钩子开场页"拔河问题。**

1. **部署链路**：本地重新 `npm run build`（`.next` 82MB→87MB，含 server/app）→ 打包（排除 node_modules/.git/.env*/data/public/media/public/videos/日志）→ scp 到 `/root/classroom-v2-deploy.tar.gz` → 服务器走唯一入口 `deploy.sh`（`sudo env DEPLOY_OPERATOR=... DEPLOY_REASON=... DEPLOY_COMMIT=e899a29`，flock 锁成功）→ tag 镜像 `classroom-v2:e899a29`，四端健康检查全 200。注意：ssh 传环境变量给 sudo 时**必须用 `sudo env VAR=... bash`**，直接 `sudo VAR=...` 会因 sudo 重置环境而报"缺少 DEPLOY_OPERATOR"。
2. **A2 媒体文件补传挂载目录**：`public/media` 是容器挂载目录（服务器 `/root/classroom-media`），11 个文件（9 个 `a2-*.html` + 2 个 mp4 `1787394753584-g5dll6.mp4`/`1787397687742-hnpe8r.mp4`）服务器原本缺失，已 scp 上传，`/api/media/file/a2-sN.html` 全 200。
3. **DB 清理重复页 + 对齐 seq**（拔河根治的最后一环）：部署后发现 DB 仍有 2 条 `a2:hook`（线上旧容器部署前重建的残留）——`ensurePages` 只清理 builtin 孤立页、**不清理重复 content 页**，且重排只在种子页增删时触发。手动删了旧文本版 `4f4b7335`（5 个 text 块），保留张老师 HTML 版 `931281cd`（embed 指向 `a2-hook-20260822131918.html`）；再把 A2 12 页 seq 手动对齐 `fullOrder`（s7 检验提交在作品墙前、s9 未来展开在 s8 三种认知前、s1 发布任务隐藏置尾）。现在 `ensurePages` 不再重建重复页，拔河已根治。
4. **A2 最终页面序列（线上已确认）**：钩子(0)→产生疑问(1)→找到方法(2)→会前准备(3)→AI团队开会(4)→自动执行(5)→检验提交(6)→作品墙(7)→未来展开(8)→三种认知(9)→最后升华(10)，发布任务 s1 隐藏(11)。

**下一步（接手先看这里）**：
- ① **未来展开页放视频**（内容页已就位 seq=8，等张老师给视频文件，界面或脚本放）
- ② 作品墙 S9 融合：张老师 S9 的"飞行上墙"动画 + 系统实时提交数据（大屏 a2:wall 现在就是网格卡片实时上墙，无动画）——方案未定，先保留系统功能页
- ③ 学生端 A2 交互重新设计（张老师定：初始只看大屏→S4 推学生端操作→作品上墙后回看大屏→梦想墙填表→升华不管学生端）

---

## 〇、最近一次交接（2026-08-22，A2 换内容：HTML 一键替换工具）

**本次会话做了一件事：环节页支持"整页换成自己写的 HTML"（A2 换内容用，commit `1b6a60c`）**：

背景：张老师要更换 A2 大部分内容（内容是自制 HTML 文件），但环节页原先只能"文字+内容+文字"，没有"清空整页 + 放一个 HTML 上去"的能力。本次补齐，两种方式任选：

1. **一键脚本**（命令行，最快）：`node scripts/replace-page-html.js <group:refKey> <html文件路径> [标题]`
   - 例：`node scripts/replace-page-html.js a2:s1 "C:\我的文件\发布任务.html"`
   - 自动做三件事：①拷 HTML 进 `public/media/`（时间戳命名）②清空该内容页全部旧内容块（只留背景）③插入一个「网页」块走 `/api/media/file/` 动态路由（无需重启即生效）。大屏 5 秒自动刷新可见。
   - refKey 对照（见脚本内注释 + `src/lib/pages.ts`）：A2 内容页 = `a2:s1` 发布任务 / `a2:s2` 产生疑问 / `a2:s3` 找到方法 / `a2:s7` 认知思考 / `a2:s9` 未来展开 / `a2:s10` 最后升华；A1 内容页 = `avatar:c1~c6` / `avatar:html07` / `avatar:c11`。
   - 只替换 kind=content 内容页；内置功能页（builtin，如 a2:hook/s4/s5/s6/wall/s8）不可整页替换，脚本会报错提示。
2. **界面操作**（鼠标）：「编辑本页」面板新增 **「🗑 清空本页」** 按钮（`DELETE /api/media?slot=page:{id}` 一键清空该页全部内容块）+ 「网页」类型**可直接上传 .html 文件**；全局媒体库下拉也加了「网页」类型 + 上传 HTML。

改动文件：`api/media/upload/route.ts`（放行 .html）、`api/media/file/[name]/route.ts`（MIME 加 text/html）、`api/media/route.ts`（DELETE 支持按 slot 批量删）、`lib/mediaStore.ts`（deleteMediaBySlot）、`components/ContentPageEditor.tsx`（清空本页 + 上传 HTML）、`components/MediaManager.tsx`（网页类型）、`scripts/replace-page-html.js`（新脚本）。

**已验证**：tsc 无错误 + build 成功 + 本地 3001 实测（HTML 上传→file 路由返回 text/html 200→脚本替换 a2:s1→批量删除→seed 回写默认块恢复），已提交 `1b6a60c`。

**下一步（接手先看这里）**：
- ⚠️ **开发阶段不上传服务器（2026-08-22 用户定铁律）**：本次改动只做了**本地 git 提交**（`1b6a60c`），**未部署线上、近期也不部署**。要试用就在本地跑（`npm run dev` 或 build 后 `next start -p 3001`）。等全部功能做好，用户说"统一部署"时再一次性上传云端。打包记得排除 `public/videos`（云端已有，勿重复传）+ `public/media`（挂载目录）+ `data` + `node_modules` + `.git` + `.env*`，但要带完整 `.next`（≥50M 含 server/app + `.next/cache`）。
- 张老师拿到工具后：本地跑 dev 或本地生产，把 A2 六个内容页的 HTML 逐个用脚本或界面换上，大屏核对。

---

## 本机位置速查

- 代码目录：`aliyun-classroom-v2\src\`（根：`C:\Users\Administrator\Documents\Default Project\aliyun-classroom-v2`）
- **线上地址**：`http://8.130.70.114:3000`
- **SSH**：`C:\Windows\System32\OpenSSH\ssh.exe` 连 `root@8.130.70.114`，密钥 `C:\Users\Administrator\.ssh\id_ed25519`（已配好，免密）
- **服务器代码目录**：`/root/classroom-v2/`
- **部署配置**：`aliyun-classroom-v2\.env.deploy`

### 收官三个大屏 HTML（都在 `aliyun-classroom-v2\public\`）

- `pain-wall.html`（痛点墙）
- `four-wings.html`（四翼展示）
- `price-reveal.html`（价格颁布）

---

## 〇、最近一次交接（2026-08-20，教师端预览小屏视频不播放）

**本次会话做了一件事：教师端双屏预览小屏里视频一律不播、只显示占位提示**（commit `9e816ec`）：

1. **预览模式机制**：URL 带 `preview=1` 时三视频组件（`VideoSource.tsx` / `A1RealityVideo.tsx` / `SmartVideo.tsx`）一律不创建 `<video>`、不预加载、不播放，只渲染占位提示「🎬 视频在真大屏播放中」。新增 `src/lib/preview-mode.ts`（`isPreviewMode()`）与 `src/components/VideoPreviewPlaceholder.tsx`（纯展示占位卡，内联 style 高 120px，无内部编号）。三组件均改为**双阶段渲染**（mounted 阶段先统一占位骨架防 hydration mismatch），预览为真时直接返回占位；组件内所有 useEffect 带 `if (preview) return;` 守卫（拉视频基址 / 自动 play 一律不执行）。SmartVideo 本身无播放 effect（autoPlay 由父传），带 preview=1 直接占位。
2. **教师端接入**：`(teacher)/teacher/page.tsx` 两处缩略预览 iframe src 各带 `&preview=1`（大屏 `/screen?sessionId=..&preview=1`、学生端 `/student?code=..&preview=1`）；真大屏/正常学生端不带参数，行为完全不变。已 `tsc --noEmit` 无错误 + `npm run build` 成功 + 本地生产 3001 四个 URL（`/screen?preview=1` `/screen` `/teacher` `/student?code=..&preview=1`）全 200。

**已部署线上（2026-08-20 20:48）**：commit `9e816ec`，tag 镜像 `classroom-v2:9e816ec`，`DEPLOY_REASON=deploy-video-local-preview-block-world-pinned`，走 deploy.sh 锁成功。**关键**：打包排除 `data/`、`public/media`、`.env*`、`node_modules`（**`data/` 必排除，否则会把线上《我的世界》/课堂数据覆盖成打包时的本地版**）；第一次包排除了 `.next/cache` 导致 `.next` 仅 4.6MB 被 deploy.sh 拒（exit 66），重打含 cache 全量（170.8MB，`.next` 157MB）。**教训：`.next/cache` 不能排除**。
- 部署后验证：容器内 `src/lib/preview-mode.ts` 就位、BUILD_ID=`wS5SzUyyVf7JvSQj8--wK` 与本地一致；`/api/world` running；`/api/video-server` 返回 `{"base":null}`（未填地址，正常回退公网）；**《我的世界》4 源码文件容器内 hash = 线上原版**（route `a5e38ca0`/engine `169da3e2`/WorldScreen `8108ce95`/WorldStudent `de199e0f`）+ `data/world/*.json` 时间戳早于部署 → 世界原封未动。
- **注意**：`/teacher` 页面是 `'use client'` 客户端渲染，静态 curl HTML 里看不到 iframe/`preview=1`（属正常）；preview 生效需浏览器验证——打开教师端双屏预览小屏应显示占位卡「🎬 视频在真大屏播放中」而非视频。

**下一步（接手先看这里）**：
- 无阻塞项；真机确认浏览器打开 `/screen?preview=1` 显示占位卡而非视频即可。

---

## 〇、次近交接（2026-08-20，上机视频本地化链路）

**本次会话做了一件事：上机视频「笔记本优先、云端回退」+ 换机迁移链路**（commit `4a1e930`，代码见下）：

1. **教室笔记本视频源优先（无需重新构建，运行时配置）**：
   - 教师端右上角新增「**视频服务器地址**」输入框（`src/components/VideoServerSettings.tsx`），保存到服务器文件 `data/video-server.txt`（API `src/app/api/video-server/route.ts`：GET/POST，校验 http(s) 前缀 + 归一化去 `/videos` 后缀）。
   - 前端统一逻辑 `src/lib/video-src.ts`（`getVideoBase()` 每次拉取不缓存 + `withBase()`）+ 新组件 `src/components/SmartVideo.tsx`（相对路径拼 base 优先，绝对 URL 直接用）。
   - 视频源全部改为三级回退「笔记本 `base/videos/` → `/videos/` → `/api/media/file/`」：`VideoSource.tsx`（新增 base state）、`A1RealityVideo.tsx`（source 2→3），内容页 3 处 `<video>` 统一换 `SmartVideo`（`MediaPlayer.tsx` / `ContentSlot.tsx` / `ContentPage.tsx`）。
   - 笔记本本地服务 `scripts/video-server.js`（纯 Node 无依赖，`npm run video-server`，端口 9123，服务 `public/videos/`，支持 Range 拖动；启动打印本机 IP 提示填教师端）。
   - 教程：`aliyun-classroom-v2/docs/上机视频本地化与换机迁移.md`（理念/填地址/跑脚本/拷文件/换机四步/回退/FAQ）。

**下一步（接手先看这里）**：
- 上机前复制 `public/videos/`（2 个 mp4，约 113MB）到跑视频服务的笔记本，`npm run video-server`，教师端填 `http://<笔记本IP>:9123` 即可全班高速播放；不填地址自动走公网。
- 此前「A1 现实视频任务回滚 + faststart 重编码」仍悬挂（见 `待办清单.md` A2 节），本次 `a1-reality.mp4` 仍是 moov 在尾部的旧版，若笔记本上拖动卡顿，需 faststart 重编码后在两端同步替换。
- 未提交改动见第三节。

---

**本次会话做了两件事：**

1. **教师端双屏预览缩放修复（最终方案）**（commit 2957700）：预览在宽屏被拉太宽/超边界，前几轮 `zoom`、`transform scale` 反复试错，最终定案 **`transform: scale` 等比缩小 + `maxWidth` 上限**。`src/components/PreviewIframe.tsx` 新增 `maxWidth` prop（scale 初始 0 不渲染、动态高度、overflow hidden、scale 上限 1 兜底）；教师端 `(teacher)/teacher/page.tsx` 大屏预览 `maxWidth={600}`、学生端 `maxWidth={380}`。已 typecheck + build + 部署 8.130.70.114，四端 200 验证通过。
   - 本链路提交：`1c91657`（整体等比缩小）→ `f8f6bc1`（Revert）→ `4b25ef5`（zoom 方案）→ `2957700`（最终 transform scale + maxWidth）。

2. **《我的世界》本地调试指南**（commit 19339b6）：`aliyun-classroom-v2/docs/my-world/04_我的世界_本地调试指南.md`（172 行：文件地图/环境准备/进 A3 方法/大屏与学生端调试/纯 HTML 改法/本地→部署流程/常见问题）。

**下一步（接手先看这里）**：
- 教师端预览缩放已部署，等张老师 Ctrl+F5 确认尺寸；要调整只需改 `(teacher)/teacher/page.tsx` 两个 `maxWidth`（600/380）。
- ⚠️ **文档滞后待补**（本次交接发现，尚未处理）：`AGENTS.md` 七章仍写《我的世界》"文档待落地"，实际 `docs/my-world/01~04` 已落地、`src/lib/world/` 引擎 + `WorldScreen.tsx`/`WorldStudent.tsx` 组件已存在；`文档索引.md` 也未登记 `docs/my-world/` 四个文档。建议接手者核实后按铁律同步三份文档。
- 长期待办（见 `待办清单.md`）：价格颁布 CTA 接真实报名链接、配图换真实截图、收官后续环节、部署加固。

**未提交改动**：见第三节。

---

## 〇、上次交接（2026-08-15 晚，历史存档）

**本次会话做完了两件事：**

1. **A2 纯展示页改成内容页**（铁律，commit 657dacf）：发布任务/产生疑问/找到方法/认知思考/未来展开/最后升华 6 个纯展示环节从功能页改成**内容页**（`pages.ts` 新增 `CONTENT_SEEDS`，seed 成 kind=content，标题+内容块存 MediaItem slot=page:{id}）；有互动的（会前准备/AI团队开会/检验提交/梦想墙）保持功能页。

2. **收官模块重建**（commit 0fe8d91 / dac0602 等）：试听课 `aliyun-classroom-v2` 新增 `CLOSING` 模块（type=closing，模板升 **A-v11**），用**环节机制**（和 A0/A1/A2/A3 完全一致，页面序列点环节投屏），三个环节页：
   - **痛点墙**（`closing:pain`，`public/pain-wall.html`）：AI坑↔自己的坑 逐张点亮（8 步），子控制条上一张/下一张
   - **四翼展示**（`closing:wings`，`public/four-wings.html`）：创造/驾驭/成长/传播 + 成长链（6 步）
   - **价格颁布**（`closing:price`，`public/price-reveal.html`）：值多少→¥39 线上小课→¥3,300-9,800 训练营（多针对行业痛点不适合小白）→转折→¥??? 悬念→¥299 白色大数字→三组价值+金色CTA（7 步）

   **关键机制**：三个 HTML 都放 `public/`，大屏 `screen/page.tsx` 按 subState 前缀渲染（`closing:pain*`/`closing:wings*`/`closing:price*`），教师端 `AvatarTeacher` 页面序列点环节投屏，每环节有子控制条（`PainWallTeacherBar`/`FourWingsTeacherBar`/`PriceRevealTeacherBar`）；HTML 内加 `window.addEventListener('message')` 接收 postMessage 控制 step。

**已部署 8.130.70.114:3000 并验证**（三端 200、CLOSING 页面序列 3 页、环节 subState 切换正常）。

**下一步（当时）**：
- 价格颁布页 **CTA「立即报名」目前是占位**（点击只打日志），需接真实报名链接
- 价格颁布页**配图用真实截图**（张老师要求：¥39 线上课、¥3,300-9,800 训练营的真实售卖页截图），目前是纯文字卡片、无图
- 收官**后续环节待填**：课程介绍、系统介绍（张老师定的收官顺序：痛点→四翼→价格→之后还要介绍课程和系统）

**部署方法**（实战验证过）：本地打包（排除 node_modules/.next/.git/.env.local/public/media）→ `scp` 到 `root@8.130.70.114:/root/classroom-v2.tar.gz` → 服务器 `bash /root/classroom-v2/update.sh`。

---

## 一、一句话现状

**这是一套线下 AI 授课的试听课方案**，核心模块现状（模板 A-v11）：

```
A0三问 → A1数字分身 → A2快速入门网站 → A3我的世界 → CLOSING 收官 → A08收尾
```

- A3 我的世界：`docs/my-world/01~04` 已落地 + `src/lib/world/` 引擎 + `WorldScreen.tsx`/`WorldStudent.tsx` + `app/api/world/*`（AI 调用复用 `llm.ts`，DeepSeek+规则回退）
- CLOSING 收官：三个 HTML 大屏页（痛点墙/四翼/价格颁布），教师端页面序列点环节投屏

---

## 二、怎么跑 / 怎么部署

```
本地开发：cd aliyun-classroom-v2 && npm run dev
构建：npm run build
部署到 8.130.70.114：打包 → scp 到 /root/classroom-v2.tar.gz → 服务器 bash /root/classroom-v2/update.sh
  （update.sh：解压→docker build→rm 旧容器→run，带 -v /root/classroom-media:/app/public/media 挂载→prisma db push→验证）
```

⚠️ 部署铁律（吃过亏，见 `AGENTS.md` 六章）：
- **部署必须走唯一入口 `/root/classroom-v2/deploy.sh`**（带 flock 互斥锁 + DEPLOY_OPERATOR/DEPLOY_REASON 审计），禁止直接 `docker cp/restart/rm/run` 或 `update.sh` 改容器
- 容器重建会丢容器内 `public/media` 上传文件 → 上传文件必须持久化到宿主机 + 走动态路由 `/api/media/file/<name>`
- 改 public 静态文件（如 ai-timeline.html）需要重新构建部署才生效（`next start` 不实时读）
- **磁盘满事件**（2026-08-13 已根治）：Docker 悬空镜像塞满 40G 磁盘→postgres 崩溃→创建课堂 500。已加三道保险（update.sh 自动 prune + 每日 4:00 cron prune + 已清 20.67GB）。再遇：`df -h /` → `docker system df` → `docker image prune -af` → `systemctl start postgresql`

---

## 三、⚠️ 未提交的改动（接手重点）

**modified（未提交）**
- `AGENTS.md`：新增「开发守则第8条·省 token 纪律」（opencode 自动压缩已配 + 结论写进文档/memory、让对话可丢弃）。内容正确，建议提交。

**untracked（未跟踪，建议清理/归档）**
- `aliyun-classroom-v2/_a0_backup.mts` / `_a0_fix.mts` / `_a0_inspect.mts`：A0 页面序列 refKey 修复的一次性 Prisma 脚本，已用完，可删。
- `aliyun-classroom-v2/dev.err`：CodeBuddy 安全删除拦截 `.next` 的报错日志，可删。
- `classroom-v2-deploy.tar.gz`（约 24MB）：部署打包产物，可删。
- `试听课文档/` 下若干素材/备份：`A1团队对比.png`、`a1梦想.png`、`ai-cognition-shift.html`、`P3与收官内容全览.md`、`_backup_a1_20260817/`、`_backup_a1_encoding_check_20260817021828/`、`图片/`、`视频/`、`ses_0192535e0ffe6ZZW7BtPSXZdCS.json`（40MB 历史对话存档，按惯例留原处不提交）。需按需归档或加入 `.gitignore`。

---

## 四、关键文件速查

| 文件 | 作用 |
|---|---|
| `AGENTS.md` / `文档索引.md` / `待办清单.md` | 项目三件套（铁律：改架构/功能后必须同步这三份） |
| `试听课文档/A0流程修改对照表.md` | A0 新流程依据（顾问建议） |
| `试听课文档/A0开场流程蓝本.md` | A0 逐页流程 + 素材缺口 |
| `试听课文档/开场情绪与环节过渡内容.md` | A0/A1/A2/A3 后放什么 + 收官弹药链接 |
| `试听课文档/全环节流程蓝本-A1至收官.md` | A1/A2/A3/收官 全环节逐屏流程蓝本 |
| `aliyun-classroom-v2/src/features/avatarLesson/config.ts` | A0_INTRO / A0_QUESTIONS_GUIDE / artImages 配置 |
| `aliyun-classroom-v2/src/components/AvatarA0Screen.tsx` | 大屏 A0 各页渲染 |
| `aliyun-classroom-v2/docs/my-world/01~04` | 《我的世界》需求/引擎/任务链/调试指南 |

---

## 五、接手第一步建议

1. 先读 `AGENTS.md`（目录/架构/铁律）→ `待办清单.md`（全部待办）→ 本文件
2. 处理未提交改动：`git status` 逐个确认，分类提交
3. 问张老师下一步做什么
