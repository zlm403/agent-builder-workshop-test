# 交接任务清单 · classroom-v2 部署加固（2026-08-15 夜）

> 背景：今晚 3 次全站 404。根因 = 容器内 `docker build` 产出的 `.next` 残缺（14M，缺 `server/app/`），`update.sh` 反复用残缺版本覆盖导致。触发因素 = 20:21–20:49 多个 AI 会话并发执行部署。
> 服务器已用本地完整 `.next`（82M）覆盖恢复，业务全 200，**当前先不要动**。
> 本清单按顺序执行，每步做完打勾。

---

## 第 1 步 · 备份（先做，防止再出事）

**目标**：把服务器关键数据 + 部署配置 + 故障证据完整备份到本地。

1.1 数据库 dump
```bash
# 在服务器执行
docker exec -e PGPASSWORD='Classroom_2026!' classroom-v2 \
  pg_dump -h 127.0.0.1 -U classroom_user -d classroom_db \
  --no-owner --no-privileges > /root/classroom_db_backup_$(date +%Y%m%d).sql
```

1.2 部署目录配置（排除大件）
```bash
tar -czf /root/classroom-v2-config.tar.gz \
  -C /root/classroom-v2 \
  --exclude=node_modules --exclude=.next --exclude=data \
  .env .env.deploy .env.example update.sh Dockerfile .dockerignore \
  package.json next.config.mjs prisma src public
```

1.3 world 运行数据
```bash
tar -czf /root/world-data.tar.gz -C /root/classroom-v2/data world
```

1.4 下载到本地保存
```bash
scp root@8.130.70.114:/root/classroom_db_backup_*.sql <本地备份目录>/
scp root@8.130.70.114:/root/classroom-v2-config.tar.gz <本地备份目录>/
scp root@8.130.70.114:/root/world-data.tar.gz <本地备份目录>/
```

---

## 第 2 步 · 提取三次故障证据（先取证，再改脚本）

**目标**：保留"谁在什么时候替换了容器"的证据，**先不要跑新 build**（避免覆盖时间戳/容器状态/构建日志）。

2.1 容器与镜像快照（先执行，存结果）
```bash
docker inspect classroom-v2 --format 'Image={{.Image}} Created={{.Created}} StartedAt={{.State.StartedAt}}'
docker images --no-trunc
docker ps -a --no-trunc
```

2.2 Docker 事件（今晚 18:00 后，查 build/创建/重启）
```bash
docker events --since '2026-08-15T18:00:00' --until '2026-08-15T23:59:59' --format '{{.Time}} {{.Action}} {{.Actor.Attributes.name}}'
```
> 注意：`docker events` 会一直监听，需配合 `--until` 或用 `timeout 30` 包裹。

2.3 SSH 登录记录（谁在何时操作）
```bash
grep 'Accepted publickey' /var/log/secure | grep 'Aug 15'
```
> 已初步确认：全部来自 IP `59.42.87.128`，同一把 ED25519 key `SHA256:FClt4tSJ...`；20:21–20:49 有几十次登录 + 3 轮 docker build；22:12–22:15 的 7 次是本会话只读诊断产生。

2.4 构建日志（update.sh 跑 build 时）
```bash
cat /tmp/docker_build.log   # 若存在；否则 docker build 重跑时 tee 保存
```

2.5 记录证据到文档
```bash
# 把 2.1–2.4 结果存入 <本地>/故障证据-20260815.md，保留原始输出
```

---

## 第 3 步 · 写加固版部署脚本 `deploy.sh`（替换 update.sh 成为唯一入口）

**目标**：服务器强制拒绝第二个部署，部署过程可审计、可校验、可回滚。

3.1 新建 `/root/classroom-v2/deploy.sh`，包含：
- `flock -n` 互斥锁（锁从开始持有到健康检查结束，拿不到锁 exit 75）
- 强制 `DEPLOY_OPERATOR` + `DEPLOY_REASON` 必填，否则 exit 64
- 审计日志 `/var/log/classroom-v2-deploy.log`（时间/操作者/目的/commit/镜像ID/容器ID/.next大小/校验/健康检查/回滚）
- 产物校验：`test -s .next/BUILD_ID && test -s .next/routes-manifest.json && test -s .next/server/app-paths-manifest.json && test -d .next/server/app`，用 Node 解析 manifest 确认关键页面
- 唯一镜像标签：`classroom-v2:$(git rev-parse --short HEAD)`（不再用 latest）
- 部署流程：独立 build → 校验 → 起新容器 → 健康检查（/teacher /screen /student /api/health /ai-cognition-shift.html 全 200）→ 全过才切换 → 失败删新留旧
- 部署前从待办清单登记"谁、何时、干什么"

3.2 部署调用方式示例：
```bash
DEPLOY_OPERATOR="AI-session-xxx" DEPLOY_REASON="修复 xxx" sudo bash /root/classroom-v2/deploy.sh
```

3.3 旧 `update.sh` 改名保留为 `update.sh.old`（不删除，供对比）。

---

## 第 4 步 · 查明容器内 build 残缺的根因

**目标**：为什么容器内 `npm run build` 产出残缺 `.next`（14M 缺 server/app），本地却正常（82M）。

4.1 对比本地与容器内 build 差异：
- 本地 `npm run build` 成功，`.next` 82M 含 `server/app/(screen)/screen`
- 容器内 docker build 显示 `Compiled successfully / exit=0`，但 `.next` 14M 缺 `server/app`

4.2 重点排查：
- `.dockerignore` 排除了 `*.cjs`、`_*.html`、`docs`——检查是否误杀构建必需文件
- 容器内 `npm install` 依赖版本与本地差异
- Docker 层缓存是否拿到旧 build 层
- build 阶段磁盘/内存是否不足（磁盘当时 62%）

4.3 用命令验证：
```bash
cd /root/classroom-v2
docker build --build-arg NEXT_PUBLIC_APP_URL="http://8.130.70.114:3000" --build-arg DATABASE_URL="$DATABASE_URL" -t classroom-v2:test . > /tmp/build_full.log 2>&1
# 然后看 build_full.log 中 RUN npm run build 段、以及 builder 阶段 .next 大小
```

---

## 第 5 步 · 规则写入文档（必须）

5.1 `AGENTS.md`：新增"部署铁律"章节——
- 服务器部署必须走 `/root/classroom-v2/deploy.sh`（唯一入口，带 flock 锁）
- 任何会话不得直接 `docker cp / restart / rm / run / update.sh` 改生产容器
- 部署必须填 `DEPLOY_OPERATOR` + `DEPLOY_REASON`
- 同一时间只能一个会话部署
- 应急恢复（本地 .next 覆盖）只作紧急手段，部署后需记录

5.2 `待办清单.md`：登记本次部署加固完成情况 + 剩余待办（如 build 根因查明）

5.3 `项目交接文档.md`：更新部署方法一节，指向新 `deploy.sh`

---

## 验收标准

- [ ] 数据库/配置/world 已备份到本地，可恢复
- [ ] 故障证据（容器/镜像/事件/SSH/构建日志）已存档
- [ ] `deploy.sh` 存在，flock 互斥锁验证过（第二个部署被拒）
- [ ] 审计日志有记录（操作者/时间/目的/校验/健康检查）
- [ ] 部署后 `.next` 完整（≥50M，含 server/app），页面全 200
- [ ] AGENTS.md / 待办清单 / 项目交接文档已同步
